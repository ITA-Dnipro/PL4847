[
  {
    "title": "Saved Companies",
    "body": "# Epic: Saved Companies — Desktop\n\n**Short description**\nImplement the desktop “My Saved / Saved Companies” experience from the mockups: saved items grid, tabs (All / Companies / Startups / Projects), filters (tags / sectors), card component with quick actions (open profile, remove saved), persistence via API, and basic analytics + QA. ",
    "labels": [
      "epic",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Tests, docs, dev runbook & CI",
    "body": "## Task 10 — Tests, docs, dev runbook & CI\n\n**Title:** qa+docs: tests, OpenAPI docs, dev runbook and CI integrations\n\n**Deliverables**\n\n* Unit tests for service layer (Task 3), Channels consumer tests (Task 4), REST endpoints (Task 5), notification hooks (Task 6), and moderation (Task 9).\n* Integration tests simulating:\n\n  * WebSocket two-client conversation exchange,\n  * Delivery/read/ack flow,\n  * Offline notification creation.\n* Fixtures: `fixtures/messaging/` with sample conversations/messages.\n* OpenAPI additions for REST endpoints and example WebSocket messages (a short spec).\n* Dev runbook `docs/dev/messaging.md` with:\n\n  * how to run Redis+Mongo locally (docker-compose snippet),\n  * how to run Channels worker,\n  * how to test WebSocket flows (sample curl / wscat commands),\n  * how to inspect Mongo data.\n* Ensure tests run in CI and include a smoketest for messaging flows.\n\n**Acceptance**\n\n* Tests pass in CI.\n* Doc committed and clear instructions to run locally.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Moderation, abuse controls & rate limiting",
    "body": "## Task 9 — Moderation, abuse controls & rate limiting\n\n**Title:** security: implement moderation features, rate limits and abuse detection\n\n**Deliverables**\n\n* Rate limits:\n\n  * Per-user message send throttle (e.g., 30 messages/min by default; configurable).\n  * Express-interest/send-message endpoints stricter limit for first N minutes.\n* Reporting & moderation:\n\n  * `POST /api/conversations/{id}/report/` to flag conversation or message (store report, notify admin).\n  * Admin endpoint to read reports and take action (remove message, ban user from messaging).\n* Profanity / malware filter:\n\n  * Plug in basic text filter or flagging pipeline (could be a simple keyword blocklist for v1).\n* Tests for throttle and report endpoints.\n\n**Acceptance**\n\n* Excessive sending results in 429.\n* Reporting endpoint persists reports and sends admin notification.\n\n**Dependencies**\n\n* Auth & admin roles, Notification admin flows.\n\n**Notes**\n\n* Document escalation path for admin.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Attachments support & validation (images/files) in chat",
    "body": "\n## Task 8 — Attachments support & validation (images/files) in chat\n\n**Title:** attachments: allow attaching uploads in messages and safe serving\n\n**Description**\n\n* Support attachments via:\n\n  * Pre-upload to existing `POST /api/uploads/` (recommended) and include `upload_id` in message payload.\n  * Server resolves `upload_id` and stores reference in message document `attachments`.\n* Validate attachment types and size server-side; reject disallowed types.\n* Serve attachment URLs via signed URLs or public S3 links (reuse Uploads service).\n* Tests for image attachment and reject invalid file.\n\n**Acceptance**\n\n* Messages created with attachments show attachment metadata and URLs in responses.\n* Clients can download attachments using returned URLs.\n\n**Dependencies**\n\n* Uploads service (existing).\n* Task 3 message service persists attachment references.\n\n**Notes**\n\n* For inline images show thumbnail url vs full url.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Read receipts, delivered status and message synchronization",
    "body": "## Task 7 — Read receipts, delivered status and message synchronization\n\n**Title:** sync: implement message status (delivered/read) updates and sync across devices\n\n**Description**\n\n* When a message is broadcast, mark `status='delivered'` for recipients whose sockets acknowledged the message (consumer should send ack).\n* When user marks messages read (via `mark_read` or automatically when viewing), update message docs and emit `read_receipt` via Channels to other participants.\n* Ensure state changes persisted and visible via REST APIs (message status fields).\n\n**Acceptance**\n\n* Unit/integration test showing:\n\n  * send_message → recipients receive event and ack,\n  * ack triggers delivered status,\n  * mark_read triggers read receipts to other participants.\n\n**Dependencies**\n\n* Task 3, Task 4, Task 5.\n\n**Notes**\n\n* Acknowledgement protocol should be lightweight (client emits `ack` with message id upon receipt).",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Notifications integration: create Notification entries & offline delivery",
    "body": "## Task 6 — Notifications integration: create Notification entries & offline delivery\n\n**Title:** notifications: create Notification records on incoming messages and wire offline delivery\n\n**Flow**\n\n* When a message is created:\n\n  * If recipient(s) connected on socket → message broadcast only.\n  * If recipient(s) offline or not focused → enqueue job to create `Notification` DB record (Postgres) and optionally send email/push.\n* Worker behavior:\n\n  * Batch email digests (configurable) or immediate emails (dev console).\n  * Add notification rows with `type='message'`, `source_user`, `conversation_id`, `snippet`.\n* Implement `GET /api/notifications/` and `PATCH /api/notifications/{id}/read/` if not present.\n\n**Acceptance**\n\n* On message to offline user, notification row created and email enqueued (console backend in dev).\n* Tests: simulate offline recipient and assert Notification exists.\n\n**Dependencies**\n\n* Notification model & worker infra (existing).\n* Task 3/4 message creation hooks.\n\n**Notes**\n\n* Respect user notification preferences (if stored). Add opt-out flag to Notification preferences later.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "REST API: conversation list, detail, send message fallback, mark read",
    "body": "\n## Task 5 — REST API: conversation list, detail, send message fallback, mark read\n\n**Title:** api: implement inbox REST endpoints (conversations & messages)\n\n**Endpoints**\n\n* `GET /api/conversations/` — list conversations for current user (paginated), include `last_message`, `unread_count`, linked `project`/`startup` info.\n* `GET /api/conversations/{conversation_id}/messages/?page=1&page_size=50` — list messages (pagination, newest last or newest first per mockup).\n* `POST /api/conversations/{conversation_id}/messages/` — fallback non-WebSocket send (same body as WS).\n* `POST /api/conversations/` — create conversation (body: participants list, optional project_id), returns conversation_id.\n* `POST /api/conversations/{conversation_id}/read/` — mark all messages as read by user.\n\n**Permissions / validation**\n\n* All endpoints require auth.\n* Validate user is participant on conversation or authorized to create a conversation (e.g., investor → startup).\n* Rate limit send endpoints.\n\n**Acceptance**\n\n* API documented in OpenAPI with example requests.\n* Tests: list, create, send message via REST (persisted to Mongo), unauthorized access returns 403.\n\n**Dependencies**\n\n* Task 3 (service layer), Task 2 (Mongo), Auth.\n\n**Notes**\n\n* Provide caching headers for conversation list if desired.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Django Channels consumer: websocket connection, auth, message events",
    "body": "\n## Task 4 — Django Channels consumer: websocket connection, auth, message events\n\n**Title:** realtime: implement WebSocket consumer(s) for messaging using Django Channels\n\n**API / behavior**\n\n* WebSocket endpoint: `ws://<host>/ws/chat/`\n* Authentication: accept JWT in `Sec-WebSocket-Protocol` or use session cookie (documented).\n* On connect: authenticate user, subscribe to channels for user (`user.<userid>`) and to conversation groups when requested.\n* Events:\n\n  * `join_conversation` `{ \"conversation_id\": \"uuid\" }` → subscribe group `conversation.<id>`.\n  * `leave_conversation`\n  * `send_message` `{ \"conversation_id\",\"body\",\"attachments\":[upload_ids] }` → validate, call service.create_message, publish `message_received`.\n  * `typing` and `presence` signals forwarded to other participants.\n  * `mark_read` `{ \"conversation_id\" }` → call service.mark_messages_read and emit `read_receipt`.\n* Outgoing events:\n\n  * `message_received` (to conversation group) with message payload.\n  * `read_receipt`, `typing`, `presence` events.\n\n**Acceptance**\n\n* Consumer unit tests (channels testing) that:\n\n  * Auth works,\n  * send_message persists the message and broadcasts to conversation participants,\n  * join/leave subscription works,\n  * read receipts emitted correctly.\n\n**Dependencies**\n\n* Redis channel layer (Channels).\n* Messaging service (Task 3).\n* Auth middleware for Channels.\n\n**Notes**\n\n* Keep consumer logic thin: validate + call service + publish event.\n* Ensure proper exception handling to avoid leaking info.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Conversation & message service layer (Python)",
    "body": "\n## Task 3 — Conversation & message service layer (Python)\n\n**Title:** service: implement server-side conversation/message service (persistence + helpers)\n\n**Description**\n\n* Implement a Python service module (e.g., `messaging.services`) that:\n\n  * Creates/loads conversations (create if not exists on first message).\n  * Persists messages to Mongo.\n  * Updates `conversations.last_message_at`.\n  * Provides recent message fetch (pagination), unread count per conversation, and mark-as-read helper.\n* Expose simple functions used by REST endpoints and Channels consumers:\n\n  * `create_message(conversation_id, sender_id, body, attachments, meta) -> message_doc`\n  * `get_conversation(conversation_id, page, page_size)`\n  * `list_conversations_for_user(user_id, page, page_size)`\n  * `mark_messages_read(conversation_id, user_id)`\n\n**Acceptance**\n\n* Unit tests for each function using a test Mongo fixture.\n* Service functions documented and used later by REST & Channels layers.\n\n**Dependencies**\n\n* Task 2 Mongo schema, Auth, Uploads.\n\n**Notes**\n\n* Validate participant membership in conversation before persisting messages.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "MongoDB schema & indexes for messages and conversations",
    "body": "\n## Task 2 — MongoDB schema & indexes for messages and conversations\n\n**Title:** db: design and implement MongoDB collections for conversations & messages\n\n**Deliverables**\n\n* Collections and example schema:\n\n  * `conversations` document:\n\n    ```json\n    {\n      \"_id\": ObjectId,\n      \"conversation_id\": \"uuid\",     // also used by app\n      \"participants\": [user_id,...],\n      \"project_id\": uuid|null,      // optional link\n      \"startup_id\": uuid|null,\n      \"created_at\": ISODate,\n      \"last_message_at\": ISODate,\n      \"meta\": {...}\n    }\n    ```\n  * `messages` document:\n\n    ```json\n    {\n      \"_id\": ObjectId,\n      \"conversation_id\": \"uuid\",\n      \"sender_id\": user_id,\n      \"body\": \"string\",\n      \"attachments\": [{ \"upload_id\": \"...\", \"url\":\"...\", \"type\":\"image\" }],\n      \"status\": \"sent|delivered|read\",\n      \"created_at\": ISODate,\n      \"meta\": {...}\n    }\n    ```\n* Create indexes:\n\n  * `messages` index on `conversation_id, created_at` (query by conversation).\n  * TTL index for retention if required (`created_at` with TTL).\n  * `conversations` index on `participants` and `last_message_at`.\n* Migration script / instructions for creating collections and indexes; include a one-time migration if moving from SQL.\n\n**Acceptance**\n\n* Index creation scripts committed and can be run locally (`mongo` script or management command).\n* Unit tests that connect to test Mongo instance and create a conversation + message.\n\n**Dependencies**\n\n* MongoDB available for dev/test environments.\n\n**Notes**\n\n* Use `pymongo` or `mongoengine` for access. For Django integration prefer a thin service layer around `pymongo` so core app logic is DB-agnostic.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Architecture & infra design doc (Channels + Mongo + Redis + Workers)",
    "body": "\n## Task 1 — Architecture & infra design doc (Channels + Mongo + Redis + Workers)\n\n**Title:** design: Messaging architecture doc (Django Channels + MongoDB + Redis + worker)\n\n**Deliverables**\n\n* Short design document covering:\n\n  * Why MongoDB for message store (fast append, BSON, flexible metadata) and Postgres for relational data (users, notifications).\n  * Channel layer: Redis (requirements and config).\n  * Websocket routing & authentication approach (JWT or session cookie).\n  * Message lifecycle: send → persist → publish to channel → create notification via background worker.\n  * Scaling & retention strategy (sharding, TTL indexes in Mongo, archiving).\n  * Security (auth checks, sanitize message content, limit attachments).\n  * Backpressure & rate limiting approach.\n* Diagrams (sequence for send/receive, flow for offline notifications).\n\n**Acceptance**\n\n* Doc committed to `docs/architecture/messaging.md`.\n* Team sign-off (one reviewer).\n\n**Dependencies**\n\n* Existing auth & user model.\n* Infrastructure: Redis, MongoDB, Celery (or chosen worker), channels.\n\n**Notes**\n\n* Provide recommended Redis and Mongo versions and sample `docker-compose` service definitions for local dev.",
    "labels": [
      "task",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Real-time Messaging & Notifications (Communication)",
    "body": "**Short description**\nImplement the messaging/communication subsystem shown in the red box: real-time one-to-one and simple multi-party chat, message inbox REST APIs, persistent message store (MongoDB), read receipts/typing/presence over WebSockets (Django Channels + Redis channel layer), integration with existing Notifications + Projects/Profiles (notify on interest/response), audit logging and admin moderation. System must work with existing auth (JWT/session), respect permissions and privacy, and scale reasonably.\n\n**Epic acceptance criteria**\n\n* Users can open a websocket connection and receive real-time messages for conversations they participate in.\n* Users can send messages via WebSocket (preferred) and fallback REST API; messages are persisted to MongoDB and reflected in the inbox.\n* Inbox REST API returns conversation list and recent messages (paginated).\n* Read receipts and presence/typing indicators are supported and emitted via channels.\n* Notifications are created for new messages (DB row) and sent to offline users via email/push worker.\n* Rate limiting, moderation (block/report), and retention policies are in place.\n* Unit & integration tests, openapi docs and a short dev runbook included.",
    "labels": [
      "epic",
      "communication"
    ],
    "state": "open"
  },
  {
    "title": "Tests, fixtures, OpenAPI docs & CI integration",
    "body": "\n## Task 10 — Tests, fixtures, OpenAPI docs & CI integration\n\n**Title:** docs+tests: add unit/integration tests, fixtures and API docs for Investment & Dashboard\n\n**Description / deliverables**\n\n* Unit tests:\n\n  * Models (Task 1), APIs (Task 2-5), notifications (Task 6), jobs (Task 7), audits (Task 8).\n* Integration tests:\n\n  * End-to-end: track → express interest → commit → project update → notification created → dashboards reflect changes.\n* Fixtures:\n\n  * `fixtures/investors.json`, `fixtures/projects_for_investor.json`, `fixtures/tracking.json`, `fixtures/investments.json`.\n* OpenAPI additions:\n\n  * Document endpoints `/api/tracking/`, `/api/interest/`, `/api/investments/`, `/api/investors/{id}/dashboard/`, `/api/investors/{id}/activity/`.\n  * Add example request/response for each endpoint.\n* CI:\n\n  * Ensure tests are executed on PRs and coverage for new modules is acceptable.\n\n**Acceptance**\n\n* Tests pass in CI.\n* API docs updated and accessible on the dev server or generated doc artifact.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Permissions, security & rate-limits for investment endpoints",
    "body": "## Task 9 — Permissions, security & rate-limits for investment endpoints\n\n**Title:** security: implement permission checks, validation and throttling for investment endpoints\n\n**Description / deliverables**\n\n* Permission rules:\n\n  * Only users with `role='investor'` may call `POST /api/investments/` and `POST /api/interest/`.\n  * Investors may only view their own dashboard & investments unless admin.\n* Add DRF throttles:\n\n  * e.g., limit `express-interest` to 5 per hour per user to avoid spam.\n  * limit `POST /api/investments/` to 20 per hour (configurable).\n* Validate inputs: amounts non-negative, currency codes ISO-4217.\n* Add tests for unauthorized access, throttle behavior, and input validation.\n\n**Acceptance**\n\n* Unauthorized or wrong-role requests return 403.\n* Throttled requests return 429.\n* Tests cover rules.\n\n**Dependencies**\n\n* Auth system & role mapping (existing).",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Audit logs for investor actions (tracking, express interest, commitments)",
    "body": "\n## Task 8 — Audit logs for investor actions (tracking, express interest, commitments)\n\n**Title:** audit: record actions in `InvestorAudit` and expose history endpoint\n\n**Description / deliverables**\n\n* Create `InvestorAudit` model capturing `actor_id`, `action` (`track_add`, `track_remove`, `express_interest`, `commit`), `target`, `payload`, `timestamp`.\n* Ensure write hooks in:\n\n  * Tracking create/delete (Task 2)\n  * Interest create (Task 3)\n  * Investment create/update (Task 4)\n* Expose `GET /api/investors/{id}/activity/` to view recent audit entries (investor-only or admin).\n* Tests for audit entries created with correct payload.\n\n**Acceptance**\n\n* Actions produce audit entries with meaningful payloads.\n* History endpoint returns entries paginated.\n\n**Dependencies**\n\n* All earlier tasks for hooks.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Aggregation job & caching: compute PortfolioSnapshot and recent activity",
    "body": "\n## Task 7 — Aggregation job & caching: compute PortfolioSnapshot and recent activity\n\n**Title:** jobs: implement periodic aggregation job that computes `PortfolioSnapshot` and recent-activity cache\n\n**Description / deliverables**\n\n* Background job (periodic Celery beat or cron job) that:\n\n  * For active investors, computes KPIs: totals committed, totals invested, project counts, gains/losses (if trackable), and stores a `PortfolioSnapshot`.\n  * Computes `recent_activity` feed (project updates, investment status changes) cached in Redis or DB table.\n* Provide management command `python manage.py compute_portfolio_snapshots --since=...` for one-off runs/reindexing.\n* Job should be idempotent and report metrics.\n\n**Acceptance**\n\n* Snapshots created and used by Dashboard endpoint (Task 5) when available.\n* Integration test that runs job on test data and asserts snapshot fields.\n\n**Dependencies**\n\n* Investment, Tracking models (Task 1), projects, notifications.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Notifications: notify investors on project updates & investment events",
    "body": "## Task 6 — Notifications: notify investors on project updates & investment events\n\n**Title:** notifications: implement project-update notifications for tracked or invested investors\n\n**Description / deliverables**\n\n* On project update (status or key field change) publish an event to a background worker:\n\n  * Worker finds interested users: those with `Tracking` for that project/startup or `Investment` records.\n  * Create `Notification` rows for each user: `type=project_update`, `payload={project_id, summary, actor_id}`\n  * Optionally send emails (email template stub) or push events (WebSocket).\n* Throttle/batching strategy:\n\n  * Avoid sending huge volumes: e.g., if project has many updates, batch into a digest per time window (configurable).\n* Provide admin config to toggle immediate or digest notifications.\n\n**Acceptance**\n\n* When a project status changes to `fundraising` or `funded`, notifications created for tracked investors.\n* Tests: simulate project update and assert Notification rows created for appropriate users (use mocks for queue).\n\n**Dependencies**\n\n* Notification model & worker infra (existing from earlier epics).\n* Projects update hook (Task 2/4) to emit events.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Dashboard API: `GET /api/investors/{id}/dashboard/` (aggregations)",
    "body": "\n## Task 5 — Dashboard API: `GET /api/investors/{id}/dashboard/` (aggregations)\n\n**Title:** api: implement Investor Dashboard aggregation endpoint\n\n**Description / API**\n\n* `GET /api/investors/{id}/dashboard/`\n\n  * Auth required; investor may only view their own dashboard (or admin).\n  * Response shape (example):\n\n    ```json\n    {\n      \"investor_id\": 42,\n      \"summary\": {\n        \"tracked_count\": 12,\n        \"projects_tracked\": 8,\n        \"total_committed\": 35000,\n        \"total_invested\": 12000,\n        \"active_investments\": 5\n      },\n      \"recent_activity\": [\n        { \"type\":\"project_update\",\"project_id\":\"...\",\"project_title\":\"...\",\"summary\":\"Status -> fundraising\",\"timestamp\":\"...\" },\n        ...\n      ],\n      \"tracked\": [ { \"target_type\":\"startup\", \"id\":\"...\", \"title\":\"...\", \"latest_project\": {...} }, ... ],\n      \"investments\": [ { \"investment_id\":\"...\", \"project\": {...}, \"amount_committed\": ... } ]\n    }\n    ```\n\n**Implementation notes**\n\n* Use `PortfolioSnapshot` (Task 1) to serve pre-computed KPIs to avoid heavy realtime queries; implement fallback realtime calc for first pass.\n* Include pagination for `tracked` and `investments` lists.\n* Add query params to request e.g., `?since=2025-07-01` for time filters.\n\n**Acceptance**\n\n* Dashboard returns KPIs, tracked items list and recent activity.\n* Tests: endpoint returns expected fields and is authorized.\n\n**Dependencies**\n\n* Task 1-4 models and Notification/activity pipeline (Task 6).\n* Aggregation job (Task 7) recommended for periodic snapshotting.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "API: Record investment commitments (no payment processing)",
    "body": "## Task 4 — API: Record investment commitments (no payment processing)\n\n**Title:** api: implement POST/GET/PATCH for `Investment` resource (`/api/investments/`)\n\n**Description / API**\n\n* `POST /api/investments/` — create commitment\n\n  * Auth investor only\n  * Body:\n\n    ```json\n    {\n      \"project_id\": \"uuid\",\n      \"amount_committed\": 10000,\n      \"currency\": \"UAH\",\n      \"meta\": { \"round\":\"seed\", \"notes\":\"term sheet pending\" }\n    }\n    ```\n  * Response: 201 with investment object.\n\n* `GET /api/investors/{id}/investments/` — list investor’s investments\n\n* `PATCH /api/investments/{id}/` — update fields (allowed: `amount_invested`, `status`, `meta`) — only owner or admin.\n\n**Business rules / validation**\n\n* Validate `project.visibility` allows investor to invest (e.g., public or investor allowed).\n* Optionally disallow investment if project `status` not `fundraising` — configurable.\n* Validate numeric inputs and currency.\n\n**Acceptance**\n\n* Investments create with status `committed`.\n* Owner can update `amount_invested` on funding progression.\n* Tests: create, unauthorized create, invalid amounts, update by non-owner forbidden.\n\n**Dependencies**\n\n* Task 1 models, Task 2 tracking (optional), Projects model and permission checks.\n\n**Notes**\n\n* This task records commitments only — **do not** integrate payment processors here unless explicitly requested later.\n",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "API: Express Interest / Contact action (`POST /api/interest/`)",
    "body": "## Task 3 — API: Express Interest / Contact action (`POST /api/interest/`)\n\n**Title:** api: implement express-interest action and link to messaging/notifications\n\n**Description / API**\n\n* `POST /api/interest/`\n\n  * Auth required (investor)\n  * Request:\n\n    ```json\n    {\n      \"project_id\": \"uuid\",\n      \"message\": \"I am interested in learning more\",\n      \"contact_opt_in\": true\n    }\n    ```\n  * Behavior:\n\n    * Create a lightweight `Interest` record (can be a typed `Investment` row with `status='expressed'` or separate model).\n    * Optionally create a `Message` to project owner (or create a `Notification`).\n    * Return 201 with interest id.\n  * Response:\n\n    ```json\n    { \"id\":\"...\", \"project_id\":\"...\", \"investor\": 42, \"status\":\"expressed\" }\n    ```\n\n**Acceptance**\n\n* Investor can express interest; project owner receives a notification entry and (optionally) an email.\n* Tests: express interest success, 401 when unauthenticated, 404 if project not found.\n\n**Dependencies**\n\n* Messaging system (Message endpoint), Notifications (Task 5), Projects model.\n\n**Notes**\n\n* Do not automatically send investor contact details unless `contact_opt_in` true.\n* Rate-limit this endpoint to prevent spam.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "API: Track / Untrack endpoints for startups & projects",
    "body": "## Task 2 — API: Track / Untrack endpoints for startups & projects\n\n**Title:** api: implement tracking endpoints `POST /api/tracking/` and `DELETE /api/tracking/{id}/` and `GET /api/investors/{id}/tracking/`\n\n**Description / API**\n\n* `POST /api/tracking/`\n\n  * Auth required\n  * Request:\n\n    ```json\n    { \"target_type\": \"startup\", \"target_id\": \"uuid\", \"source\": \"manual\" }\n    ```\n  * Behavior: create `Tracking` if not exists (idempotent), return 201 with tracking object.\n  * Response:\n\n    ```json\n    { \"id\":\"...\", \"investor\": 42, \"target_type\":\"startup\",\"target_id\":\"...\", \"created_at\":\"...\" }\n    ```\n\n* `DELETE /api/tracking/{id}/`\n\n  * Auth required, only owner may delete\n  * Response: 204\n\n* `GET /api/investors/{id}/tracking/?type=startup&page=1&page_size=20`\n\n  * Return paginated list of tracked items with basic target data (title, slug, thumbnail) — join to `StartupProfile` or `Project`.\n\n**Permissions & validation**\n\n* Investor may only manage their own tracking.\n* Validate `target_id` exists; on missing return 404/400.\n\n**Acceptance**\n\n* Creating tracking is idempotent (re-POST returns existing).\n* Deleting removes entry and is accessible only to owner.\n* Tests: create, duplicate create, list, delete, unauthorized attempts return 403.\n\n**Dependencies**\n\n* Task 1 models, Profiles & Projects read endpoints to fetch target metadata.\n\n**Notes**\n\n* Consider returning `is_tracked` boolean for endpoints used from other APIs (profile/project detail).",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Data model: `Investment`, `Tracking`, and `PortfolioSnapshot` models + migrations",
    "body": "\n## Task 1 — Data model: `Investment`, `Tracking`, and `PortfolioSnapshot` models + migrations\n\n**Title:** models: add Investment, Tracking and PortfolioSnapshot models and migrations\n\n**Description / deliverables**\n\n* Implement backend models (Django ORM example fields shown). Add migrations.\n\nExample models:\n\n```py\nclass Tracking(models.Model):\n    id = UUIDField(primary_key=True)\n    investor = ForeignKey(User, on_delete=CASCADE, related_name='tracking')\n    target_type = CharField(choices=('startup','project'))\n    target_id = UUIDField()  # FK via generic relation or separate FK fields\n    created_at = DateTimeField(auto_now_add=True)\n    source = CharField(max_length=32, null=True)  # 'manual','suggestion','import'\n    meta = JSONField(null=True)  # optional metadata\n\n    class Meta:\n        unique_together = ('investor','target_type','target_id')\n        indexes = [Index(fields=['investor']), Index(fields=['target_type','target_id'])]\n```\n\n```py\nclass Investment(models.Model):\n    id = UUIDField(primary_key=True)\n    investor = ForeignKey(User, on_delete=CASCADE, related_name='investments')\n    project = ForeignKey(Project, on_delete=PROTECT, related_name='investments')\n    status = CharField(choices=('committed','transferred','returned','cancelled'), default='committed')\n    amount_committed = DecimalField(max_digits=18, decimal_places=2)\n    amount_invested = DecimalField(max_digits=18, decimal_places=2, default=0)\n    currency = CharField(max_length=3, default='UAH')\n    created_at = DateTimeField(auto_now_add=True)\n    updated_at = DateTimeField(auto_now=True)\n    meta = JSONField(null=True)  # notes, terms, round info\n```\n\n```py\nclass PortfolioSnapshot(models.Model):\n    id = UUIDField(primary_key=True)\n    investor = ForeignKey(User, on_delete=CASCADE)\n    computed_at = DateTimeField()\n    projects_count = IntegerField()\n    total_committed = DecimalField(...)\n    total_invested = DecimalField(...)\n    summary = JSONField()  # precomputed KPIs\n```\n\n**Acceptance**\n\n* Migrations created and applied without errors.\n* Unit test that creates tracking and investment rows and enforces unique constraints.\n\n**Dependencies**\n\n* `User` model (Auth), `Project` model (Projects epic).\n* DB access & migrations infra.\n\n**Notes**\n\n* Use generic relation or explicit FK depending on DB design. UUID primary keys recommended for public objects.\n* Add indexes for queries by investor and project.",
    "labels": [
      "task",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Investment Tracking & Investor Dashboard (backend)",
    "body": "**Short description**\nImplement the backend piece marked in the diagram: allow investors to **track/save startups & projects**, **express interest**, **record commitments/investments**, and provide an **Investor Dashboard** API that aggregates portfolio KPIs, tracked items, and recent updates. Include notifications for project updates, audit logging, permissions, tests and docs. This epic depends on Profiles, Projects, Auth, Uploads, Notifications and Search (already scoped/implemented earlier).\n\n**Why / value**\nInvestors need a single place to follow startups, record & monitor investments, and receive updates — core to engagement and the product’s investor value proposition.\n\n**Epic acceptance criteria**\n\n* Investors can track/untrack startups/projects and that data is persisted.\n* Investors can record an investment commitment (not a payment gateway — record only).\n* A dashboard API `GET /api/investors/{id}/dashboard/` returns aggregated portfolio metrics (counts, totals, recent activity, tracked items), and a list of recent project updates.\n* Project updates trigger notifications to investors who track or invested.\n* All write operations are permission-checked (investor = authenticated investor user / owner).\n* Audit log exists for express interest, tracking, and commitments.\n* Unit & integration tests, fixtures and OpenAPI docs are present; CI green.",
    "labels": [
      "epic",
      "investment"
    ],
    "state": "open"
  },
  {
    "title": "Admin & moderation: project moderation tools",
    "body": "## Task 10 — Admin & moderation: project moderation tools\n\n**Goal:** Provide admin endpoints and moderation flows.\n\n**Deliverables**\n\n* Admin endpoints:\n\n  * `GET /api/admin/projects/` (list with filters)\n  * `PATCH /api/admin/projects/{id}/moderate/` { action: approve/reject/flag }\n* On reject, send email to startup with reason.\n* Bulk operations: CSV import/export (optional).\n* Tests for admin actions and audit logs.\n\n**Acceptance**\n\n* Admin can change project visibility, delete or restore soft-deleted projects, and moderate content.\n* Email templates and audit records produced for moderation actions.\n\n**Dependencies**\n\n* Auth with admin role, Task 1/2.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Tests, fixtures, API docs & CI",
    "body": "## Task 9 — Tests, fixtures, API docs & CI\n\n**Goal:** Add unit & integration tests, sample fixtures and OpenAPI documentation for all new endpoints.\n\n**Deliverables**\n\n* Unit tests for models (Task 1) and serializers.\n* API tests for endpoints (Task 2) including auth/permissions, edge cases (invalid status), and file attachments (Task 3).\n* Integration tests for notifications (Task 5) and indexing (Task 6) using mocks or test queues.\n* Add fixtures: `fixtures/projects.json` with 5 sample projects (various statuses).\n* Update OpenAPI / Swagger docs with example requests/responses for:\n\n  * create project\n  * update project\n  * project detail\n  * project attachments\n  * project history\n* CI: ensure tests run on PRs and coverage for new modules is acceptable.\n\n**Acceptance**\n\n* Tests added and passing in CI; docs updated and accessible.\n\n**Dependencies**\n\n* All previous tasks.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Audit logs, change history and undo support",
    "body": "## Task 8 — Audit logs, change history and undo support\n\n**Goal:** Record changes for compliance and enable simple revert.\n\n**Deliverables**\n\n* Create `ProjectAudit` entries on create/update/delete with `changes` JSON showing diffs, `actor_id`, `timestamp`.\n* `GET /api/projects/{id}/history/` endpoint returning paginated audit list.\n* `POST /api/projects/{id}/revert/` to revert to a previous version (owner only) — optional but recommended.\n* Tests: audit created on update, revert restores fields and logs revert action.\n\n**Acceptance**\n\n* For any update, an audit row exists with `before` and `after` values.\n* History endpoint returns entries and revert works (if implemented).\n* Tests pass.\n\n**Dependencies**\n\n* Task 1 model and Task 2 endpoints.\n\n**Notes**\n\n* Avoid storing sensitive data in audit diffs (sanitize).",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Permissions, roles & ownership checks",
    "body": "\n## Task 7 — Permissions, roles & ownership checks\n\n**Goal:** Centralize and test permission model for project actions.\n\n**Deliverables**\n\n* Permission rules enforced:\n\n  * `create` requires authenticated user and ownership of startup (user linked to `StartupProfile`).\n  * `update/delete` restricted to owners or org-admins.\n  * Investors and other users can view public projects; private/unlisted require auth & explicit permission.\n* Implement decorator/middleware or DRF permission classes (e.g., `IsStartupOwner`, `IsOrgAdmin`).\n* Add tests for unauthorized access, escalation attempts.\n\n**Acceptance**\n\n* Attempt to edit another startup's project returns 403.\n* Role checks for organization admins work.\n\n**Dependencies**\n\n* Auth & Profile ownership mapping.\n\n**Notes**\n\n* Document permission rules in API docs.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Search indexing & sync for projects",
    "body": "## Task 6 — Search indexing & sync for projects\n\n**Goal:** Ensure projects are searchable and facets reflect project data.\n\n**Deliverables**\n\n* Indexing integration: on create/update/delete/publish change, send document to search engine (Meilisearch/Elasticsearch/Postgres FTS).\n* Document shape for index:\n\n  ```json\n  {\n    \"id\":\"project-uuid\",\n    \"title\":\"Handmade Chairs\",\n    \"short_description\":\"...\",\n    \"description\":\"...\",\n    \"startup_id\":\"uuid\",\n    \"startup_name\":\"Handmade Co\",\n    \"tags\":[\"wood\",\"furniture\"],\n    \"location\": \"Lviv\",\n    \"status\":\"fundraising\",\n    \"thumbnail_url\":\"...\"\n  }\n  ```\n* Background worker to reindex existing projects (one-off migration task).\n* API to trigger reindex for a single project (admin use).\n\n**Acceptance**\n\n* After creating a project with `visibility=public`, it appears in `GET /api/search/` results (mock search or actual search).\n* Tests: indexing task called on create/update; search returns expected document (integration test with a test instance).\n\n**Dependencies**\n\n* Search service infra, Task 1/2, Task 4 for status filtering.\n\n**Notes**\n\n* If search infra not yet chosen, provide adapter pattern and a simple Postgres FTS fallback.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Notifications & event publishing on project changes",
    "body": "## Task 5 — Notifications & event publishing on project changes\n\n**Goal:** Notify followers/investors on important project events (created, status changed, update).\n\n**Deliverables**\n\n* Event emitter when project created/updated/status changed:\n\n  * Publish to message queue (Celery/Rabbit/Kafka) or trigger async task.\n  * Create `Notification` DB entries for interested users (followers of startup, investors who saved/tracked).\n* Implement endpoint `GET /api/notifications/` and `PATCH /api/notifications/{id}/read/` (if not already done).\n* Email notification templates for major events (project launched, moved to fundraising, funded).\n* Throttle/batching logic for avoid email storm (e.g., summary notifications, or 1 per user per 24h per project).\n\n**Acceptance**\n\n* Creating a project enqueues notification tasks; workers create Notification rows and send emails (console/staging).\n* Tests: when status changes to `fundraising`, followers receive notification entries (mock queue).\n\n**Dependencies**\n\n* Auth, Profiles (followers list), Messaging/Notifications infra, Email templates (email service).\n* Project models (Task 1) and CRUD (Task 2).\n\n**Notes**\n\n* For initial delivery, implement worker that writes Notification rows; actual email sending can be a separate follow-up but include template stubs.\n* Add idempotency to event handling to avoid duplicate notifications on retries.\n",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Business rules & status transitions (and validation)",
    "body": "\n## Task 4 — Business rules & status transitions (and validation)\n\n**Goal:** Implement and enforce project lifecycle rules and calculated fields.\n\n**Key rules**\n\n* Status enum: `idea` → `mvp` → `fundraising` → `funded` → `closed`. Only allowed transitions or explicit admin override.\n* When `raised_amount >= target_amount` and status is `fundraising`, auto-set status to `funded` (or set `is_funded` flag).\n* Prevent setting `raised_amount` > `target_amount` unless `allow_overfunding` flag true.\n* If status set to `funded`, record `funded_at`.\n* If project visibility changes to `public`, index it in search (Task 6).\n\n**API**\n\n* `PATCH /api/projects/{id}/status/` (optional shortcut) — body `{ \"status\":\"fundraising\" }`\n\n**Acceptance**\n\n* Server enforces valid transitions and business rules with clear error messages (400).\n* Unit tests for each allowed transition, auto-funding behavior, invalid transitions.\n\n**Dependencies**\n\n* CRUD API (Task 2), notifications (Task 5), indexing (Task 6).\n\n**Notes**\n\n* Implement state machine (e.g., django-fsm) or explicit checks in service layer to keep logic centralized.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Attachments & uploads integration for projects",
    "body": "\n## Task 3 — Attachments & uploads integration for projects\n\n**Goal:** Allow attaching images/docs to projects using the existing uploads service.\n\n**API & behavior**\n\n* Option A (separate upload): frontend `POST /api/uploads/` → returns `{id, url}` then include `attachment_ids` in project create/update.\n* Option B (multipart): allow `multipart/form-data` to `POST /api/startups/{id}/projects/` with files inline.\n\n**Deliverables**\n\n* Backend support to accept attachment IDs and create `ProjectAttachment` rows (order/caption metadata).\n* Validate attachment types (images for thumbnails, pdf/docx for pitch decks) and size limits.\n* Serve attachment URLs in `GET /api/projects/{id}/` response.\n\n**Acceptance**\n\n* Uploading via `POST /api/uploads/` + creating project with attachments works end-to-end.\n* Attachments are returned with metadata in project detail.\n* Tests: attach image, invalid file type rejected, attach large file rejected.\n\n**Dependencies**\n\n* Central Uploads service/endpoint (should already exist or be created).\n* Task 1 models.\n\n**Notes**\n\n* For dev environment, use local storage; for prod use S3 with lifecycle rules.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "CRUD API: Projects (create / list / retrieve / update / delete)",
    "body": "\n## Task 2 — CRUD API: Projects (create / list / retrieve / update / delete)\n\n**Goal:** Implement REST endpoints for project management.\n\n**API endpoints**\n\n* `POST /api/startups/{startup_id}/projects/` — create (owner only)\n\n  ```json\n  {\n    \"title\":\"Handmade Chairs\",\n    \"short_description\":\"Oak chairs...\",\n    \"description\":\"<p>full HTML</p>\",\n    \"target_amount\": 50000,\n    \"currency\":\"UAH\",\n    \"tags\":[\"wood\",\"furniture\"],\n    \"visibility\":\"public\"\n  }\n  ```\n\n  Response `201` returns project object.\n\n* `GET /api/startups/{startup_id}/projects/` — list (public view if startup public)\n\n* `GET /api/projects/{id}/` — retrieve project detail\n\n* `PATCH /api/projects/{id}/` — partial update (owner only)\n\n* `PUT /api/projects/{id}/` — full update (owner only)\n\n* `DELETE /api/projects/{id}/` — delete (soft delete recommended)\n\n**Permissions & validation**\n\n* Only authenticated users with ownership of `startup_id` can create/update/delete.\n* Public retrieval allowed for `visibility=public` projects (or if profile is public). Private/unlisted enforce auth/ownership or specific tokens.\n* Server-side validation: `raised_amount` ≤ some rule? (accept for updates but validate business rules in Task 4).\n\n**Acceptance**\n\n* API endpoints implemented with serializers, proper HTTP codes, and error messages.\n* Test cases: create success, unauthorized create (401/403), owner update works, non-owner update forbidden.\n\n**Dependencies**\n\n* Auth (token + owner resolution)\n* Models from Task 1\n* Upload endpoint for attachments (Task 3) if attachments are passed as IDs.\n\n**Notes**\n\n* Use `IsOwnerOrReadOnly` permission class.\n* Return `Location` header on create.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Data model & migrations: `Project` and related tables",
    "body": "\n## Task 1 — Data model & migrations: `Project` and related tables\n\n**Goal:** Define DB schema and create initial migrations.\n\n**Deliverables**\n\n* Django models (or ORM of choice):\n\n  ```py\n  class Project(models.Model):\n      id = UUIDField(primary_key=True)\n      startup = ForeignKey(StartupProfile, on_delete=CASCADE, related_name='projects')\n      title = CharField(max_length=255)\n      slug = SlugField(unique=True)\n      short_description = TextField(max_length=500)\n      description = TextField()  # sanitized HTML or Markdown storage\n      status = CharField(choices=PROJECT_STATUS, default='idea')\n      target_amount = DecimalField(...)\n      raised_amount = DecimalField(default=0)\n      currency = CharField(max_length=3, default='UAH')\n      tags = ArrayField/TextManyToMany (tags model)\n      visibility = CharField(choices=('public','private','unlisted'), default='public')\n      created_at = DateTimeField(auto_now_add=True)\n      updated_at = DateTimeField(auto_now=True)\n  ```\n* Attachment model (if not reusing uploads):\n\n  ```py\n  class ProjectAttachment(models.Model):\n      id, project FK, upload FK, type (image/pdf), order, caption\n  ```\n* Audit table for project changes:\n\n  ```py\n  class ProjectAudit(models.Model):\n      project FK, user FK, timestamp, changes JSON\n  ```\n\n**Acceptance**\n\n* Migrations added and apply cleanly.\n* Basic unit test that creating a Project row works and relations are enforced.\n\n**Dependencies**\n\n* `StartupProfile` model must exist (Profiles epic).\n* Uploads service / `Upload` model for attachments (Task 3).\n\n**Notes**\n\n* Use UUIDs for public resources to avoid enumeration.\n* Add DB indexes on `startup_id`, `slug`, `status`, `tags` (or tag join table) for queries.",
    "labels": [
      "task",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Project Management (backend)",
    "body": "**Short description**\nImplement backend support for Projects (the red-marked box): create, read, update, delete, status transitions and attachments for startup projects; business rules, notifications on updates, indexing for search, audit logging, and admin moderation. This epic delivers the full server-side capabilities that let startups manage projects and investors track them.\n\n**Why / value**\nProjects are the primary product offers startups present to investors. Backend must be robust, secure and integrate with auth, profiles, uploads, notifications and search.\n\n**Epic acceptance criteria**\n\n* Startups can create/update/delete their own projects via authenticated APIs.\n* Projects support attachments (images, pitch decks), status (idea / mvp / fundraising / funded / closed), and financial fields (target, raised).\n* Status transitions enforce business rules and trigger notifications to followers/investors.\n* Projects are indexed for search and appear in investor dashboards and profile pages.\n* All changes are auditable (who/when/what changed).\n* Unit/integration tests and API docs exist; CI passes.",
    "labels": [
      "epic",
      "project"
    ],
    "state": "open"
  },
  {
    "title": "Docs & UX: Empty-state copy, help CTAs and privacy notes",
    "body": "**Title:** docs: add empty-state copy, help CTAs and privacy note for saved items\n\n**Purpose:** Provide clear user guidance when the saved list is empty and document privacy handling for saved items.\n\n**Checklist**\n\n* [ ] Create UI copy for empty states (no saves / filtered result empty), including CTAs: “Browse companies”, “Import from CSV?” (optional), and “Learn how saved items are used”.\n* [ ] Short privacy blurb explaining who can see saved items (private by default) and how to remove them.\n* [ ] Add short README docs: API usage examples, rate-limit policies for save/remove, and test plan for QA team.\n* [ ] Add example mock fixture `mock/saved-42.json` for frontend devs.\n\n**Acceptance criteria**\n\n* Empty state copy and CTAs implemented on frontend.\n* Docs committed under `docs/saved/` with API examples and test steps.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Analytics, performance & QA: tracking + E2E smoke",
    "body": "**Title:** qa: instrument analytics for saved actions + add E2E smoke test\n\n**Purpose:** Track user interactions and add a simple E2E smoke test to verify the main flow: page open, view saved cards, remove saved item.\n\n**Checklist**\n\n* [ ] Emit analytics events: `saved_page_view`, `saved_item_view`, `saved_item_remove`, `filter_changed`, `tab_changed`.\n* [ ] Server-side log metric for saved counts per user (for dashboard/monitoring).\n* [ ] E2E (Cypress/Playwright) test: login (or mock auth), open `/saved`, assert presence of cards, click remove and assert card removed and undo works.\n* [ ] Performance check: initial load should return within a reasonable time for 12 items (document target, e.g., p95 < 300ms for cached query).\n* [ ] Accessibility quick check: cards keyboard reachable and controls labelled.\n\n**Acceptance criteria**\n\n* Analytics events sent in dev/staging.\n* E2E smoke test passes in CI.\n* Accessibility has no critical failures.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Filters & sort bar (tags, sector, saved order)",
    "body": "**Title:** frontend: implement filters bar and saved sort options\n\n**Purpose:** Allow users to filter their saved list by tag/sector and sort by `saved_at` newer/older or alphabetical.\n\n**Checklist**\n\n* [ ] UI for filter chips populated by tags returned from backend (or a small `/api/users/{id}/saved/filters/` endpoint).\n* [ ] Sort control (dropdown) with options: `Newest`, `Oldest`, `A→Z`.\n* [ ] Integrate with list fetching (update query params and re-fetch).\n* [ ] Debounce filter changes, show active-filter pill group that can be cleared with one click.\n* [ ] Tests for filter/sort interactions and URL-sync.\n\n**Acceptance criteria**\n\n* Filters and sort change results and update URL so the view is bookmarkable.\n* Clear All removes filters and updates results.\n* Tests pass.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: `SavedCard` component (actions + visuals)",
    "body": "**Title:** frontend: implement `SavedCard` component used in grid\n\n**Purpose:** Reusable card for saved items: thumbnail, title, short description, tags, saved star icon (active), quick actions (open profile, remove saved), and saved timestamp.\n\n**Checklist**\n\n* [ ] Implement component props matching backend shape.\n* [ ] Show thumbnail (with `alt`), title (link to `/startups/:slug` or `/companies/:slug`), tags, and saved_at formatted.\n* [ ] Action icons: open/view button and Remove (trash or star toggle). Remove triggers `DELETE` saved endpoint and removes the card from UI on success.\n* [ ] Confirm removal: show small confirm modal/inline undo (UX choice — implement undo toast for 5s).\n* [ ] Accessibility: buttons keyboard-focusable and labelled (`aria-label`).\n* [ ] Unit tests for UI states and remove flow (mock API).\n\n**Acceptance criteria**\n\n* Clicking Remove deletes saved item via API and card disappears with undo toast.\n* Clicking title navigates to profile.\n* Tests pass.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Saved Page shell + tabs + pagination (route `/saved`)",
    "body": "**Title:** frontend: implement Saved page shell with tabs, results grid and pagination\n\n**Purpose:** Build the page scaffolding and connect to backend feed (or mocks). Tabs: All | Companies | Startups | Projects. Desktop layout per mockup: toolbar with tabs & filter chips, grid of card components, and pagination or Load More.\n\n**Checklist**\n\n* [ ] Route `/saved` and `SavedPage` React component.\n* [ ] Tabs that set `type` query param and re-fetch results.\n* [ ] Grid displays 3–4 cards per row depending on desktop layout (use mock from API).\n* [ ] Pagination: either numbered pages or “Load more” button (implement one; prefer `Load more`).\n* [ ] Loading skeleton while fetching and empty-state card when none saved.\n* [ ] Tests: tab switching, fetch calls, load-more behaviour, empty-state.\n\n**Acceptance criteria**\n\n* Tabs update results and URL query `?type=company` so deep links work.\n* Load-more appends additional cards.\n* Tests pass.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `POST /api/users/{id}/saved/` and `DELETE /api/users/{id}/saved/{saved_id}/` — save/un-save actions",
    "body": "**Title:** backend: implement save and remove saved item endpoints\n\n**Purpose:** Allow user to add/remove saved items (companies/projects/startups). Backend must validate target existence and ownership rules.\n\n**API spec**\n\n* Save:\n\n```\nPOST /api/users/42/saved/\n{ \"target_type\": \"company\", \"target_id\": 123 }\n```\n\nResponse `201`:\n\n```json\n{ \"saved_id\": 987, \"saved_at\":\"2025-08-01T12:00:00Z\" }\n```\n\n* Remove:\n\n```\nDELETE /api/users/42/saved/987/\n```\n\nResponse `204 No Content`\n\n**Checklist**\n\n* [ ] Implement endpoints with auth and validation: target exists; cannot save own company? (specify behaviour).\n* [ ] Return id and saved timestamp on create.\n* [ ] Prevent duplicates: idempotent save (return existing saved record if already saved).\n* [ ] Unit tests: create, duplicate create, delete, invalid id, auth.\n\n**Acceptance criteria**\n\n* Save endpoint returns `201` (or existing) and `DELETE` removes saved entry.\n* Duplicate saves are idempotent.\n* Tests included.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/users/{id}/saved/` — list saved items (paginated + tabs)",
    "body": "**Title:** backend: implement `GET /api/users/{id}/saved/` returning paginated saved items with type filter\n\n**Purpose:** Provide the saved items data feed used by the page. Support optional `type` param (`company`, `startup`, `project`) and pagination.\n\n**API spec**\n\n```\nGET /api/users/42/saved/?page=1&page_size=12&type=company\n```\n\nResponse example:\n\n```json\n{\n  \"count\": 30,\n  \"next\": \"…\",\n  \"previous\": null,\n  \"results\": [\n    {\n      \"id\": 987,\n      \"saved_id\": 123,\n      \"type\": \"company\",\n      \"title\": \"Association of Retailers Ukraine\",\n      \"slug\": \"raiu\",\n      \"thumbnail_url\": \"https://…/thumb.jpg\",\n      \"short_description\": \"About the org…\",\n      \"location\": \"Kyiv\",\n      \"tags\": [\"retail\"],\n      \"saved_at\": \"2025-08-01T12:00:00Z\"\n    }, ...\n  ]\n}\n```\n\n**Checklist**\n\n* [ ] Implement authenticated endpoint that returns only the requesting user’s saved items (or admin).\n* [ ] Support `type` filter and pagination (default page_size=12).\n* [ ] Include minimal fields for card rendering (title, slug, thumbnail, short_description, tags, saved_at).\n* [ ] Unit tests: list, pagination, `type` filter, 401 for unauthenticated.\n* [ ] Document endpoint in API spec.\n\n**Acceptance criteria**\n\n* Authenticated user gets paginated list and filter by `type` works.\n* 401 returned when not authenticated.\n* Tests pass.",
    "labels": [
      "task",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Saved Companies",
    "body": "**Short description**\nImplement the desktop “My Saved / Saved Companies” experience from the mockups: saved items grid, tabs (All / Companies / Startups / Projects), filters (tags / sectors), card component with quick actions (open profile, remove saved), persistence via API, and basic analytics + QA. Each sub-issue below is independent so one student can own it.\n",
    "labels": [
      "epic",
      "saved list"
    ],
    "state": "open"
  },
  {
    "title": "Analytics, performance & QA",
    "body": "**Title:** analytics: instrument search events, optimize performance, and add QA tests\n\n**Purpose:** Track search metrics, optimize backend queries for reasonable p95, and add E2E smoke test that verifies search happy path.\n\n**Checklist**\n\n* [ ] Emit analytics events for: search executed (`q`, filters, results_count`), suggestion clicked, card clicked, filter changes.\n* [ ] Add server-side logging/metrics for search latency; set target (e.g., p95 < 300ms for cached queries).\n* [ ] Add caching layer for frequent queries / facets (Redis or search engine cache).\n* [ ] E2E smoke test (Cypress/Playwright): open search page, run query, apply a filter, click a result — assert navigation.\n* [ ] Accessibility quick-check: ensure form input label, result count announces for screen readers, and controls keyboard-accessible.\n\n**Acceptance criteria**\n\n* Analytics events fire on user actions (verified in dev).\n* E2E smoke test passes in CI.\n* Documented perf target and basics of tuning (indexing, caching).\n* Accessibility checks for core elements pass.\n",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Empty-state, suggestions and “No results” UX",
    "body": "**Title:** frontend: implement helpful empty state with suggestions and alternative actions\n\n**Purpose:** When search returns zero results show helpful copy, suggested queries/tags, and CTA (create profile / broaden search / contact support).\n\n**Checklist**\n\n* [ ] Design and implement empty-state UI matching mockup (icon, headline, suggestions list).\n* [ ] Fetch suggestions (related tags / query corrections) from backend or provide client-side heuristics.\n* [ ] Provide CTAs: “Clear filters”, “Try popular tags”, and “Create a new listing” (link to registration).\n* [ ] Add unit tests to assert empty-state rendering and CTA behaviors.\n\n**Acceptance criteria**\n\n* No-results search shows suggestions and CTAs.\n* Clicking suggested tag runs new search.\n* Tests pass.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Filters / Facets sidebar and active-filters bar",
    "body": "**Title:** frontend: implement filters/facets UI and active-filters bar\n\n**Purpose:** Render facets returned from backend with counts; allow users to select multiple filters, clear filters, and see active filters in a compact bar.\n\n**Checklist**\n\n* [ ] `FiltersSidebar` component consumes `facets` from `GET /api/search/`.\n* [ ] Support multi-select facets (tags, locations, status).\n* [ ] Active-filters bar above results shows selected filters with a clear (x) and a global Clear All.\n* [ ] URL-sync filters so page reload preserves selection.\n* [ ] Apply debounce when refetching results on filter change (avoid flooding API).\n* [ ] Unit tests for selection and URL state sync.\n\n**Acceptance criteria**\n\n* Selecting filters updates results and `facets` counts update accordingly.\n* Active filters displayed and removable.\n* Tests pass.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: `SearchCard` component + accessible markup",
    "body": "**Title:** frontend: implement `SearchCard` UI used in results grid\n\n**Purpose:** Small reusable card that displays thumbnail, title, snippet, tags, location and CTA buttons (View / Save / Follow). Must be keyboard-friendly and accessible.\n\n**Checklist**\n\n* [ ] Build `SearchCard` component with props matching backend result shape.\n* [ ] Thumbnail image with `alt`, lazy-loading, and fallback placeholder.\n* [ ] Highlighted snippet area renders HTML-safe highlight (`<em>`).\n* [ ] Tag chips clickable and trigger tag filter.\n* [ ] Include action buttons: `Save` (calls stub API), `View` link to profile/project.\n* [ ] Add keyboard focus states and ARIA labels for action buttons.\n* [ ] Snapshot/unit tests covering states (with highlight, without image).\n\n**Acceptance criteria**\n\n* Cards render correctly and are keyboard accessible.\n* Clicking tag triggers a filter update on `SearchPage`.\n* Tests pass.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Search page layout & results grid",
    "body": "**Title:** frontend: implement Search page and results grid (route `/search?q=...`)\n\n**Purpose:** Build the main page UI from mockups: search header, results grid (cards), results count, sorting control, pagination / load-more button.\n\n**Checklist**\n\n* [ ] Create `SearchPage` route and component; read URL params (`q`, filters, page, sort).\n* [ ] Fetch data from `GET /api/search/` and render `SearchCard` list (12 per page default).\n* [ ] Implement sorting control and update URL on change (history push).\n* [ ] Implement pagination: prefer `Load more` (incremental fetch) and support page param for deep links.\n* [ ] Loading / empty / error states: skeleton cards during load, helpful empty-state UI (see Issue 5).\n* [ ] Add unit tests for URL-sync, fetch handling, load-more behavior.\n\n**Acceptance criteria**\n\n* Visiting `/search?q=craft` shows results and correct count and cards rendered.\n* Changing sorting or filters updates results and URL.\n* Load-more appends next page of results.\n* Tests pass.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/search/suggestions/` — autocomplete & query suggestions",
    "body": "**Title:** backend: implement autocomplete / suggestions API\n\n**Purpose:** Provide live suggestions (names, tags, recent queries) for the search input.\n\n**API spec**\n\n* Request:\n\n```\nGET /api/search/suggestions/?q=cra&limit=8\n```\n\n* Response:\n\n```json\n{ \"suggestions\": [ { \"type\":\"tag\",\"value\":\"craft\" }, { \"type\":\"startup\",\"value\":\"CraftMerge\" }, ... ] }\n```\n\n**Checklist**\n\n* [ ] Implement fast prefix suggestions (search-engine-backed or cached popular values).\n* [ ] Include type attribute (tag/startup/project/query).\n* [ ] Throttle to avoid abuse; document rate-limits.\n* [ ] Tests for suggestion ranking and empty query behavior.\n\n**Acceptance criteria**\n\n* Typing partial query returns relevant suggestions within tolerance.\n* Suggestions distinguished by type for UI icons/labels.\n* Tests pass.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/search/` — core search endpoint (text + filters + pagination)",
    "body": "**Title:** backend: implement `GET /api/search/` (text search + filters + pagination)\n\n**Purpose:** Provide a single API that returns paginated search results across startups/projects with filtering, sorting and highlighting snippets.\n\n**API spec**\n\n* Request:\n\n```\nGET /api/search/?q=craft&page=1&page_size=12&sort=relevance&tag=pottery&location=Lviv&status=active\n```\n\n* Response (200):\n\n```json\n{\n  \"count\": 123,\n  \"next\": \"...\",\n  \"previous\": null,\n  \"results\": [\n    {\n      \"id\": 11,\n      \"type\": \"startup\",          // or \"project\"\n      \"title\": \"Handmade Co\",\n      \"slug\": \"handmade-co\",\n      \"short_description\": \"Woodwork & ceramics...\",\n      \"thumbnail_url\": \"...\",\n      \"tags\": [\"craft\",\"pottery\"],\n      \"location\": \"Lviv\",\n      \"score\": 0.92,\n      \"highlight\": \"We make <em>craft</em> ceramics...\"\n    },\n    ...\n  ],\n  \"facets\": {\n    \"tags\": { \"pottery\": 12, \"wood\": 8 },\n    \"location\": { \"Lviv\": 20, \"Kyiv\": 30 },\n    \"status\": { \"active\": 50, \"inactive\": 10 }\n  }\n}\n```\n\n**Checklist**\n\n* [ ] Implement search using database full-text or search engine (Elastic/Meilisearch/PG trigram) — choose and document.\n* [ ] Support filters: tags, location, status, category.\n* [ ] Support sorting options: `relevance`, `newest`, `most_followed`.\n* [ ] Return facet counts for UI filters.\n* [ ] Add basic snippet/highlight support.\n* [ ] Add unit tests for text search, filters, pagination, and facet counts.\n* [ ] Document query params and sample responses (OpenAPI).\n\n**Acceptance criteria**\n\n* `GET /api/search/` returns a valid paginated response with `results` and `facets`.\n* Filtering & sorting return consistent, testable results.\n* Tests cover main cases.",
    "labels": [
      "task",
      "search"
    ],
    "state": "open"
  },
  {
    "title": "Search results page (landing + filters + results)",
    "body": "**Short description**\nImplement the site search experience shown in the mockups: full search page (query input + controls), results grid (cards), filters/facets, sorting, pagination / “load more”, empty-state, suggestions/autocomplete, and necessary backend endpoints. Frontend and backend work should be decomposed into independent issues so students can work in parallel.\n\n**Epic acceptance criteria**\n\n* Users can search by free-text and see paginated results (cards) with images, title, short description, tags, and CTA.\n* Filters/facets (category/tags/location/status) and sorting work and update results without full page reload.\n* Empty/search-no-results state shows helpful suggestions and CTA.\n* Autocomplete API and UI provide live suggestions.\n* Analytics events emitted for search queries and clicks.\n* Basic performance & accessibility checks pass and an E2E smoke test verifies the main flow.\n\n---\n",
    "labels": [],
    "state": "open"
  },
  {
    "title": "QA & E2E: profile edit flow coverage and accessibility",
    "body": "**Title:** qa: add E2E tests and accessibility checks for profile editing flows\n\n**Description**\nAdd automated E2E tests that cover edit, draft, upload, publish, conflict and history viewing. Run accessibility checks (axe/Lighthouse) on the edit and preview pages. Add manual QA checklist.\n\n**Checklist**\n\n* [ ] E2E test: open edit page, update main info, upload logo, save draft, preview, publish — assert states and API calls.\n* [ ] E2E test: simulate conflict (mock second session update) and assert conflict UI displayed.\n* [ ] E2E test: test file upload flow (or mock S3).\n* [ ] Accessibility scan for the edit page and preview; fix high-priority issues (form labels, aria-live for save status).\n* [ ] Add QA guide `docs/qa/profile-edit.md` with manual verification steps and screenshots.\n\n**Acceptance criteria**\n\n* E2E tests run in CI and pass for the profile edit workflow.\n* Accessibility has no critical failures; fixes applied where needed.\n* QA doc added.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Audit log & versioning for profile changes",
    "body": "**Title:** backend: implement audit trail and optional version history for profiles\n\n**Description**\nRecord profile edits in an audit log (who, when, changed fields). Provide an endpoint to fetch change history and optional ability to revert to previous version.\n\n**Checklist**\n\n* [ ] Create `ProfileAudit` model: `profile_id, user_id, timestamp, changes (json)`.\n* [ ] On successful profile save/publish, create audit entry containing changed fields and old/new values.\n* [ ] `GET /api/profiles/{id}/history/` returns list of audit entries (paginated).\n* [ ] Optional: `POST /api/profiles/{id}/revert/` to restore previous version (owner-only).\n* [ ] Tests for audit entries on updates and history endpoint.\n\n**Acceptance criteria**\n\n* Audit entries created on updates with clear `changes` payload.\n* History endpoint returns chronological entries.\n* Revert endpoint (if implemented) restores fields and creates corresponding audit record.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Permissions, roles, and publish workflow",
    "body": "**Title:** backend: implement ownership/roles & publish workflow (draft → published)\n\n**Description**\nDefine and enforce permissions: who can edit/publish a profile (owner, organization admins). Implement publish action that validates profile completeness rules and toggles published state.\n\n**Checklist**\n\n* [ ] Add `Profile` fields: `is_published`, `published_at`, `published_by_id` and `draft_saved_at`.\n* [ ] Implement permission checks: only owner or organization admin can publish.\n* [ ] Implement validation rules for publish (required fields present, at least one image or pitch deck if policy requires).\n* [ ] Endpoint `POST /api/profiles/{id}/publish/` to attempt publish; returns 200 or 400 with missing fields list.\n* [ ] Unit tests for permissions and publish validations.\n\n**Acceptance criteria**\n\n* Unauthorized users cannot publish (403).\n* Publish endpoint enforces required fields and returns helpful list of missing items.\n* On success, `is_published` true and `published_at` set.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Drafts, autosave and conflict handling",
    "body": "**Title:** frontend: implement autosave & draft management with conflict handling\n\n**Description**\nAutosave profile edits to a draft resource so users won’t lose work. Provide explicit Save Draft action. Handle edit conflicts if another session publishes changes (show diff/merge prompt).\n\n**Checklist**\n\n* [ ] Implement `POST /api/profiles/{id}/drafts/` or reuse PATCH with `is_draft=true` flag.\n* [ ] Autosave on field blur or periodic interval (e.g., every 30s) to save a draft.\n* [ ] UI shows draft saved timestamp and unsaved changes indicator.\n* [ ] On publish, save final data and clear draft.\n* [ ] Conflict detection: when saving/publishing, API returns conflict if remote `updated_at` > client `last_read_at`; show a merge/overwrite prompt with basic diff highlights.\n* [ ] Unit tests for autosave trigger and conflict handling flows.\n\n**Acceptance criteria**\n\n* Autosave creates/updates a draft on backend and UI indicates saved status.\n* If another user has updated the profile, frontend detects conflict and prompts user with options (view remote changes, overwrite).\n* Drafts persist and can be recovered after reload.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Backend & Frontend: Media uploads (logo, hero, attachments)",
    "body": "**Title:** uploads: implement file upload endpoint(s) and integrate into profile editing UX\n\n**Description**\nSupport uploading images and documents. Validate MIME type and size; store on S3-compatible storage in production and local storage in dev. Frontend should show preview thumbnails, upload progress, and integrate file IDs/URLs into profile payload.\n\n**Checklist (backend)**\n\n* [ ] Endpoint `POST /api/uploads/` that accepts multipart file and returns `{ id, url, mime_type, size }`.\n* [ ] Validate allowed types (images: jpg/png/svg; docs: pdf/docx) and max sizes (configurable).\n* [ ] Store files to configured storage (S3 in prod, local for dev).\n* [ ] Add ACL/visibility metadata (public/private) if needed.\n* [ ] Unit tests for upload success & invalid file types.\n\n**Checklist (frontend)**\n\n* [ ] Integrate upload widget in `MediaUpload` component; show thumbnail/filename and progress.\n* [ ] On successful upload add returned file id/url to profile save payload.\n* [ ] Support replacing/removing files and confirming removal before saving.\n* [ ] Tests or Storybook stories demonstrating upload UI.\n\n**Acceptance criteria**\n\n* Upload endpoint returns file metadata and stored file is accessible by returned URL.\n* Frontend shows preview and attaches uploaded files to profile updates.\n* Tests for upload behaviors included.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Profile Edit Page & form components",
    "body": "**Title:** frontend: implement profile edit page and modular form components\n\n**Description**\nCreate the profile edit UI: form sections (Main info, About, Products/Projects management, Contact details, Tags, Visibility settings, Media upload area). Include client-side validation, rich-text input for About, and ability to save draft or publish.\n\n**Checklist**\n\n* [ ] Route `/profile/edit` (or `/startups/:id/edit`) and `ProfileEdit` page.\n* [ ] Modular components: `MainInfoForm`, `RichTextAbout`, `ProjectsListEditor`, `ContactForm`, `TagsInput`, `VisibilitySelect`, `MediaUpload`.\n* [ ] Use controlled forms with client validation (required fields, email/URL formats, password if present).\n* [ ] Provide Save Draft and Publish buttons with clear states and success toasts.\n* [ ] Preview button opens read-only preview of the profile (on same route or modal).\n* [ ] Show inline server validation errors returned from API.\n* [ ] Unit tests for major components.\n\n**Acceptance criteria**\n\n* User can edit all fields, save draft and publish; UI shows success states and handles server errors.\n* Preview shows how the published profile will look (render sanitized HTML).\n* Component tests in place.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/profiles/{id}/` and `PUT/PATCH /api/profiles/{id}/` (profile read & update)",
    "body": "**Title:** backend: implement profile read and update endpoints\n\n**Description**\nImplement API endpoints to read a public profile and to update profile fields for the owner. The update endpoint should accept JSON (and/or multipart when files included) and enforce validation, ownership, and atomic updates.\n\n**Checklist**\n\n* [ ] `GET /api/profiles/{id}/` returns full public profile payload (name, slug, about_html, short_description, contact, website, tags, stats, media URLs, visibility).\n* [ ] `PUT /api/profiles/{id}/` and `PATCH /api/profiles/{id}/` accept updates and require authentication + ownership (permission class `IsOwnerOrReadOnly`).\n* [ ] Server-side validation for required fields, URL formats, tags, numeric fields.\n* [ ] Support update semantics: PATCH for partial changes, PUT for full replace.\n* [ ] Tests for success, invalid data (400), unauthorized (401/403).\n* [ ] Return updated resource in response.\n\n**Acceptance criteria**\n\n* Owner can successfully PATCH profile fields and receive 200 with updated object.\n* Non-owner receives 403 for write attempts.\n* Invalid input returns 400 with field-level errors.\n* Unit tests included.",
    "labels": [
      "task",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "Profile editing flow (create & edit startup / organization profile)",
    "body": "**Short description**\nAllow authenticated users to create and maintain a complete profile: main info (name, short description), About / Story (rich text), Products & Projects, contact details, media (logo, hero image, pitch deck), tags, visibility settings, and preview + publish workflow. Include server-side validation, file handling, permission checks, autosave/draft support, audit trail, and QA/E2E coverage.\n\n* **Acceptance criteria (epic-level)**\n\n  * Authorized user can open the profile edit UI, change fields, upload files, save draft, preview, and publish profile.\n  * Backend endpoints validate and persist data; files are stored and served.\n  * Permission model prevents unauthorized edits.\n  * Change history/audit exists for profile updates.\n  * E2E smoke test covers create/edit/preview/publish and passes in CI.\n",
    "labels": [
      "epic",
      "profile editing"
    ],
    "state": "open"
  },
  {
    "title": "QA / E2E: Visual QA + accessibility + one smoke test",
    "body": "**Title:** qa: visual review, accessibility checks and E2E smoke test for profile page\n\n**Purpose:** Ensure the desktop profile page meets visual expectations and basic accessibility; add a simple E2E smoke test.\n\n**Checklist**\n\n* [ ] Visual QA checklist with screenshots comparing page sections (hero, about, sidebar, projects).\n* [ ] Run `axe` or Lighthouse accessibility checks for the page; fix critical issues (missing alt, heading order).\n* [ ] E2E smoke test (Playwright/Cypress): open profile page, assert hero present, About heading present, click one project card and assert navigation (or modal).\n* [ ] Add QA doc `docs/qa/profile-page.md` with manual verification steps.\n\n**Acceptance criteria**\n\n* E2E smoke test passes on CI for the profile fixture.\n* Accessibility has no critical A/A11y failures.\n* QA document added.\n",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend + Backend: Messaging CTA — open compose modal & `POST /api/messages/`",
    "body": "**Title:** messaging: implement Message CTA and `POST /api/messages/` API (frontend modal + backend endpoint)\n\n**Purpose:** Allow logged-in users to click “Message” on profile header, open a modal, compose a short message and send to profile owner.\n\n**API spec (example)**\n\n* `POST /api/messages/`\n\n```json\n{ \"to_user_id\": 123, \"subject\":\"Hello\", \"body\":\"Short message...\" }\n```\n\n* Response: `201` with message id\n\n**Checklist (backend)**\n\n* [ ] Simple `POST /api/messages/` that creates message row (requires auth).\n* [ ] Validate message length and recipient exists.\n* [ ] Unit tests for auth and create.\n\n**Checklist (frontend)**\n\n* [ ] Implement `MessageModal` that opens from profile header, includes subject + body, client-side validation.\n* [ ] Submit to `/api/messages/` (or mock) and show success toast and close modal.\n* [ ] If user not logged-in, CTA opens login modal or shows prompt to login.\n* [ ] Tests for modal UI and submission.\n\n**Acceptance criteria**\n\n* Logged-in user can send message; backend creates message.\n* Not-logged-in users see prompt to login.\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Sidebar Info Card & Actions (desktop)",
    "body": "**Title:** frontend: implement `SidebarCard` (contact, tags, stats, quick actions)\n\n**Purpose:** Provide right-side card with contact info, tags, statistic badges, and quick CTA buttons (Follow, Message, Save).\n\n**Checklist**\n\n* [ ] `SidebarCard` component showing: contact buttons (email/phone), website link, tags list (clickable), followers count, follow/save button state.\n* [ ] Clicking follow/save toggles UI state and calls mock endpoint `/api/startups/{id}/follow/` (for now stub).\n* [ ] Ensure telephone/email links use proper `tel:` / `mailto:` attributes.\n* [ ] Accessibility: buttons keyboard-focusable and labels for screen readers.\n* [ ] Unit tests for UI interactions (mock API).\n\n**Acceptance criteria**\n\n* Sidebar displays contact and meta info.\n* Follow/save interactions update UI and call API stub.\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: About / Products / Story content components",
    "body": "**Title:** frontend: implement `About`, `ProductsList`, and `Story` components for main content\n\n**Purpose:** Render sanitized `about_html`, structured Products/Services list, and Story block with headings and internal anchors.\n\n**Checklist**\n\n* [ ] `About` component: render `about_html` safely (use DOMPurify or server-side sanitization).\n* [ ] `ProductsList` component: render list/cards of products/services (data either from profile response or separate `projects` endpoint).\n* [ ] `Story` component: render timeline/text sections as per mockup.\n* [ ] Add smooth scroll / in-page anchor support for right-side quick links (optional).\n* [ ] Unit tests to ensure content rendering and sanitization.\n\n**Acceptance criteria**\n\n* Rich content displays correctly (paragraphs, lists, headings).\n* No raw unsafe HTML executes (sanitization).\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Profile Page shell & layout (desktop)",
    "body": "**Title:** frontend: implement `ProfilePage` desktop layout & route `/startups/:slug`\n\n**Purpose:** Build the page skeleton and layout pieces (hero area, two-column layout: content + right sidebar, footer).\n\n**Checklist**\n\n* [ ] Route `/startups/:slug` and `ProfilePage` component.\n* [ ] Fetch profile from `GET /api/startups/{id}/` (or use mock if backend not ready).\n* [ ] Render hero (hero_image + logo + name) and header action buttons (Follow, Message, Save) as clickable UI (can be no-op / stub).\n* [ ] Two-column grid: left main content; right sidebar with contact card & quick stats.\n* [ ] Provide loading and error states (skeleton loader and 404 page).\n* [ ] Basic CSS desktop styles (container width, margins) consistent with mockup.\n* [ ] Unit/snapshot test for rendering with fixture data.\n\n**Acceptance criteria**\n\n* Visiting `/startups/{slug}` shows hero, header actions, and two-column layout.\n* Loading state shown while fetching; error state on fetch fail.\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/startups/{id}/projects/` — projects listing (paginated)",
    "body": "**Title:** backend: implement `GET /api/startups/{id}/projects/` for profile’s projects/cards\n\n**Purpose:** Provide projects (or offers) shown on the profile page.\n\n**API spec (example)**\n\n* `GET /api/startups/123/projects/?page=1&page_size=6`\n* Response:\n\n```json\n{\n  \"count\": 3,\n  \"results\": [\n    { \"id\": 11, \"title\":\"Handmade Chairs\",\"status\":\"active\",\"thumbnail\":\"...\", \"short_desc\":\"...\" },\n    ...\n  ]\n}\n```\n\n**Checklist**\n\n* [ ] Viewset or list API returning project summary fields.\n* [ ] Pagination (default page_size configurable).\n* [ ] Filtering by status or tag optional.\n* [ ] Unit tests for list and pagination.\n\n**Acceptance criteria**\n\n* API returns paginated list and includes expected fields for cards.\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `GET /api/startups/{id}/` — profile detail (public)",
    "body": "**Title:** backend: implement `GET /api/startups/{id}/` returning full public profile data\n\n**Purpose:** Provide the profile data used to render the page (hero image, name, badge, about text, contact, tags, stats, links).\n\n**API spec (example)**\n\n* `GET /api/startups/123/`\n* Response:\n\n```json\n{\n  \"id\": 123,\n  \"company_name\": \"Handmade Co\",\n  \"slug\": \"handmade-co\",\n  \"hero_image_url\": \"https://.../hero.jpg\",\n  \"logo_url\": \"https://.../logo.png\",\n  \"short_description\": \"Woodwork & ceramics\",\n  \"about_html\": \"<p>Full about text...</p>\",\n  \"contact\": { \"email\":\"info@handmade.co\", \"phone\":\"+380...\" },\n  \"website\":\"https://handmade.example\",\n  \"tags\": [\"craft\",\"pottery\"],\n  \"followers_count\": 42,\n  \"projects_count\": 3,\n  \"created_at\":\"2025-07-01T12:00:00Z\"\n}\n```\n\n**Checklist**\n\n* [ ] Serializer & viewset for read-only public profile.\n* [ ] Return sanitized HTML or Markdown-rendered safe content for `about_html`.\n* [ ] Include counts/stats used in UI (followers_count, projects_count).\n* [ ] Unit tests for response schema and missing-profile (404).\n\n**Acceptance criteria**\n\n* `GET /api/startups/{id}/` returns 200 with fields above.\n* 404 for unknown id.\n* Tests pass.",
    "labels": [
      "task",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Desktop Profile Page (Startup / Organization)",
    "body": "**Short description:**\nImplement the desktop profile page per mockup: large hero image, header with actions (follow / message / save), main content (About, Products/Services, Story), right sidebar with quick info (contact, tags, stats), projects/cards listing, and footer. Backend endpoints to serve profile data and related projects; frontend React components to render and handle interactions. Desktop only.\n\n**Epic acceptance criteria**\n\n* `/startups/{id}` page renders on desktop matching mockup layout and contains: hero, profile header actions, About section, Products/Services, Projects list, sidebar contact & stats, footer.\n* All API endpoints required by frontend exist or have mock fixtures.\n* Client-side accessibility basics (semantic headings, alt text, focusable actions).\n* Smoke E2E test that opens a profile page and asserts presence of hero, About, and at least one project.",
    "labels": [
      "epic",
      "profile page"
    ],
    "state": "open"
  },
  {
    "title": "Security & Ops: spam protection, rate-limits, CSRF/CORS docs",
    "body": "**Title:** security: implement bot protection (reCAPTCHA or throttles), rate-limits and deployment docs\n\n**Description:**\nAdd bot protection during registration (choose reCAPTCHA or strict rate-limits), define throttle policies, set CORS policy, and document production email provider & secrets. Provide runbook for mass-signup detection.\n\n**Checklist**\n\n* [ ] Decide and implement bot protection (reCAPTCHA v2/v3 or hCaptcha) or strict throttling.\n* [ ] Backend rate-limits: per IP and per email attempts for registration and resend endpoints.\n* [ ] Configure CORS to allow only frontend origin.\n* [ ] Document required env secrets for email provider and recaptcha.\n* [ ] Add monitoring/alerting rules for unusual signup spikes and email bounce rate.\n\n**Acceptance criteria**\n\n* Registration endpoint protected by bot mitigation or documented rate-limits.\n* CORS rules documented and enforced.\n* Runbook / monitoring doc added `docs/security/registration.md`.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "QA / E2E: Registration + Verify flows & accessibility checks",
    "body": "**Title:** qa: E2E tests for registration + verify + file upload; accessibility review\n\n**Description:**\nAdd E2E flows (Cypress/Playwright) that cover registration of both roles, email verification, file upload flows, and negative cases (invalid fields, weak password). Also run axe/Lighthouse accessibility for the pages.\n\n**Checklist**\n\n* [ ] E2E test: Startup register → intercept verification email token → verify → login.\n* [ ] E2E test: Investor register → verify → login.\n* [ ] E2E test: file upload during registration (mock S3 or use local storage).\n* [ ] Accessibility scan for registration pages.\n* [ ] Add tests to CI as smoke tests for PRs touching auth/registration.\n\n**Acceptance criteria**\n\n* E2E tests run in CI and pass (use DB fixtures/mock email capture).\n* Accessibility report has no critical issues.\n* QA docs in `docs/qa/registration.md`.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Email uniqueness check & live validation (inline)",
    "body": "**Title:** frontend: implement inline email uniqueness check and user-friendly duplicate handling\n\n**Description:**\nProvide inline feedback for email availability while typing (debounced check). If email already exists, show friendly guidance: “If this is your account — try login or reset password” (do not leak whether role/profile exists beyond safe UX).\n\n**Checklist**\n\n* [ ] Debounced call to `GET /api/auth/check-email/?email=...` (or `POST`), endpoint can be mocked.\n* [ ] Show inline message for available vs. already-registered.\n* [ ] If already-registered -> show actions: `Login` or `Forgot password` links.\n* [ ] Ensure this check has rate-limiting server-side to avoid abuse.\n\n**Acceptance criteria**\n\n* Email field shows debounce availability state.\n* If email exists, show helpful non-leaking guidance and action buttons.\n* Tests cover debounce + UI states.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend + Backend: Terms, legal consent, newsletter opt-in & checkbox persistence",
    "body": "**Title:** consent: implement Terms & Privacy checkbox + newsletter opt-in and store consent metadata\n\n**Description:**\nEnsure registration requires explicit acceptance of Terms/Privacy (checkbox). Optionally include newsletter opt-in. Save consent with timestamp & IP in DB for compliance.\n\n**Checklist**\n\n* [ ] Frontend: add required Terms & Privacy checkbox (link opens modal or page).\n* [ ] Add optional newsletter opt-in checkbox.\n* [ ] Backend: Accept consent fields in registration payload and store consent timestamp and IP in user record or `Consent` model.\n* [ ] Add unit tests and a migration for consent fields.\n\n**Acceptance criteria**\n\n* Registration blocked unless required checkbox is checked.\n* Consent saved with timestamp and source IP in DB.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend + Frontend: File uploads (logo, pitch deck) handling",
    "body": "**Title:** file: implement file upload handling for registration profiles (logo & pitch deck)\n\n**Description:**\nSupport uploads during registration or via later profile edit. Validate types/size server-side, store files on S3-compatible storage (or local dev storage), and ensure the frontend can upload with `multipart/form-data` while showing progress.\n\n**Checklist (backend)**\n\n* [ ] Endpoint or support in registration endpoint for multipart uploads (or accept upload via separate endpoint `/api/uploads/`).\n* [ ] Validate file types (e.g., logos: jpeg/png/svg; pitch deck: pdf/docx) and max size (e.g., 10 MB).\n* [ ] Store files in configured storage (S3 or dev local).\n* [ ] Return file URL or file id in response.\n\n**Checklist (frontend)**\n\n* [ ] Show file chooser, preview (logo thumbnail), and upload progress.\n* [ ] On file selection, either upload immediately and attach returned id to registration payload or include in multipart registration call.\n* [ ] Show inline errors for invalid file types/sizes.\n* [ ] Unit tests for upload UI behavior (mock backend).\n\n**Acceptance criteria**\n\n* Uploads accepted and stored; upload errors returned with field-level messages.\n* Uploaded image preview displays in UI.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Registration UI — Investor (desktop)",
    "body": "**Title:** frontend: implement Investor registration page (desktop) — fields, validation & states\n\n**Description:**\nImplement Investor registration form per mockup: email, password, password confirm, investor name, investor type (individual/fund), investment preferences (checkboxes/tags), minimum investment, contact fields, terms checkbox, and register button.\n\n**Checklist**\n\n* [ ] Create `RegisterInvestor` page/component.\n* [ ] Client-side validation for required fields and numeric fields (min investment).\n* [ ] Tag/checkbox UI for sectors of interest.\n* [ ] Submit to `/api/auth/register/` or mock and show confirmation messaging.\n* [ ] Tests for validation and form submit handling.\n\n**Acceptance criteria**\n\n* Investor form can be completed and returns success message instructing to verify email.\n* Form shows inline validation and prevents submit when invalid.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Registration UI — Startup (desktop)",
    "body": "**Title:** frontend: implement Startup registration page (desktop) — fields, validation & states\n\n**Description:**\nImplement desktop Startup registration form matching mockup: inputs for email, password, password confirm, company name, short pitch, website, contact, file upload (logo/pitch deck optional), checkbox for terms, and register button. Include UI states: idle, field errors (red), success/confirmation, server-side errors.\n\n**Checklist**\n\n* [ ] Build `RegisterStartup` route/page and component.\n* [ ] Client-side validation: required fields, email format, password match & min length, URL format.\n* [ ] File input for pitch deck/logo — show chosen filename and client-side validations for size/type.\n* [ ] Checkbox for Terms & Privacy (required).\n* [ ] Submit POST to `/api/auth/register/` or mock; show spinner and success confirmation instructing to check email.\n* [ ] If backend returns field errors, display inline.\n* [ ] Accessibility: labels, aria-live for messages.\n* [ ] Unit tests for validation & states.\n\n**Acceptance criteria**\n\n* Valid form submits and shows \"check your email\" confirmation.\n* Invalid input shows inline errors; attempting submit with unchecked terms blocks submit.\n* File upload UI displays selected file; client-side rejects invalid type/size.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Email verification after registration + `POST /api/auth/resend-verification/`",
    "body": "**Title:** backend: implement email verification endpoint & resend\n\n**Description:**\nProvide endpoint to verify email tokens and a resend endpoint. Tokens must be time-limited and single-use. Resend endpoint should be rate-limited.\n\n**API**\n\n* `POST /api/auth/verify-email/` `{ \"token\": \"<token>\" }` -> 200 activate account\n* `POST /api/auth/resend-verification/` `{ \"email\": \"...\" }` -> 200 (silent on non-existing email)\n\n**Checklist**\n\n* [ ] Implement token generation & verification (time-limited).\n* [ ] Mark user active on successful verification.\n* [ ] Implement `resend` with rate-limiting per email/IP.\n* [ ] Unit tests: verify success, expired token, resend throttle.\n\n**Acceptance criteria**\n\n* Verification link activates user and redirects to UI route or returns 200 for API-based flow.\n* Resend returns 200 and throttles multiple calls.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `POST /api/auth/register/` — registration endpoint (startup & investor)",
    "body": "**Title:** backend: implement `POST /api/auth/register/` for startup and investor accounts\n\n**Description:**\nCreate a single registration endpoint that supports role-specific fields (role: `startup` or `investor`). Validate server-side, create inactive user, create relevant profile (StartupProfile/InvestorProfile), send verification email, and return minimal safe response.\n\n**API spec (example)**\n\n* **Request:** `POST /api/auth/register/`\n\n```json\n{\n  \"email\": \"alice@example.com\",\n  \"password\": \"P@ssw0rd!\",\n  \"role\": \"startup\",\n  \"company_name\": \"Handmade Co\",\n  \"short_pitch\": \"Woodwork & ceramics\",\n  \"website\": \"https://example.com\",\n  \"contact_phone\": \"+380...\" \n}\n```\n\n* **Response 201:**\n\n```json\n{ \"id\": 123, \"email\": \"alice@example.com\", \"detail\": \"Verification email sent.\" }\n```\n\n* **Errors:** 400 validation errors, 409 duplicate email\n\n**Checklist**\n\n* [ ] Serializer and view created; accepts role-specific fields.\n* [ ] Create user as inactive until email verification (or mark as verified if policy differs).\n* [ ] Create profile model entry (StartupProfile or InvestorProfile) and link to user.\n* [ ] Trigger verification email (link with token).\n* [ ] Enforce server-side password rules.\n* [ ] Return 201 with non-sensitive message; avoid leaking whether email already exists (see anti-enumeration policy).\n* [ ] Unit tests for happy path, validation, duplicate email behavior.\n\n**Acceptance criteria**\n\n* Valid request → 201 and verification email queued.\n* Invalid data returns field-level 400s.\n* Duplicate email returns either 200 (silently) or 409 per chosen anti-enumeration policy — document chosen approach.\n* Tests pass.",
    "labels": [
      "task",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Desktop Registration Flow (Startup & Investor)",
    "body": "**Description (short):**\nImplement the desktop registration experience from the mockups: Startup and Investor registration pages, client-side validations & UX states, backend registration API, email verification/resend, file uploads (pitch deck/logo), consent/terms handling, duplicate-email check, and QA/E2E tests. Provide secure token handling and production-ready docs.\n\n**Acceptance criteria (epic-level):**\n\n* Both startup & investor desktop registration flows implemented, wired to backend endpoints or working with mocks.\n* Email verification and resend flow implemented.\n* File uploads processed by backend (size/type validated).\n* Basic bot protection (reCAPTCHA or rate-limit) and anti-account-enumeration.\n* E2E smoke tests for registration + verification run in CI.",
    "labels": [
      "epic",
      "registration flow"
    ],
    "state": "open"
  },
  {
    "title": "Security & Docs: deployment guidance, token TTLs, monitoring & runbook",
    "body": "**Title:** security: document secure settings, TTLs, monitoring and incident steps\n\n**Description (short):**\nDocument production recommendations: token TTL, throttle limits, email provider secrets, CSRF/CORS notes, monitoring/alerting and incident runbook for password reset abuse.\n\n**Checklist**\n\n* [ ] Recommend token TTL (e.g., 1 hour) and refresh/expiry policies.\n* [ ] Throttling policy examples: e.g., 5 requests/hour per email, 10 requests/hour per IP.\n* [ ] Guidance on token storage scheme (stateful vs stateless) and implications.\n* [ ] CORS and CSRF config notes if using cookies for auth.\n* [ ] Monitoring: metrics to expose and thresholds to alert (excess reset requests, many failed confirms).\n* [ ] Add runbook: how to revoke tokens, how to respond to suspected abuse.\n* [ ] Commit `docs/security/restore-password.md`.\n\n**Acceptance criteria**\n\n* Document committed to repo and reviewed by at least one team member.\n* Clear production-ready recommendations and actionable runbook.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "QA & E2E: End-to-end tests + accessibility for restore flow",
    "body": "**Title:** qa: E2E and accessibility for Restore Password flow\n\n**Description (short):**\nAdd E2E tests (Cypress/Playwright) that run confirm scenarios and accessibility checks (axe/lighthouse). Cover happy path and at least these error scenarios: expired token, reused token, weak password.\n\n**Checklist**\n\n* [ ] E2E test: request reset → intercept email link OR read token from DB fixture → visit `/reset-password?uid=...&token=...` → set new password → login with new password.\n* [ ] E2E test: expired token → assert friendly error and `Resend` CTA.\n* [ ] E2E test: reused token → assert failure.\n* [ ] Accessibility check (axe) for the restore pages; fix critical issues.\n* [ ] Add tests to CI pipeline or PR checks.\n\n**Acceptance criteria**\n\n* E2E tests run in CI for PRs affecting auth and pass.\n* Accessibility scan reports no blocking issues.\n* QA docs (test steps and screenshots) added to `docs/qa/restore-password.md`.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: UX polish — deep links, auto-redirect, and resend shortcut",
    "body": "**Title:** frontend: deep-link behavior, auto-redirect and \"Resend reset email\" UX\n\n**Description (short):**\nPolish UX per mockup: when user clicks email link the token is preloaded and input focused; on success show brief confirmation and (optionally) auto-redirect to login; provide an easy resend flow if token invalid/expired.\n\n**Checklist**\n\n* [ ] When token present: hide token input and auto-focus password field.\n* [ ] On success: show success component and auto-redirect to `/login` after configurable N seconds (allow immediate manual click).\n* [ ] If invalid token: show `Resend reset email` button that opens `/forgot-password` with email pre-filled if available.\n* [ ] Provide clear copy & accessible announcements.\n* [ ] Unit tests for redirect and resend flow.\n\n**Acceptance criteria**\n\n* Clicking link lands on form with token applied and correctly focused.\n* On invalid token user can with one click open reset request page with email prefilled.\n* Auto-redirect occurs but user can cancel/navigate manually.\n* Tests pass.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Restore Password Page (desktop) — form & token handling",
    "body": "**Title:** frontend: implement Restore Password page (parse token, set new password)\n\n**Description (short):**\nCreate the desktop UI component and route that reads token & uid from URL (or allows paste), validates new password client-side, posts to confirm endpoint, and displays success / error states per mockups (invalid/expired token, weak password).\n\n**Checklist**\n\n* [ ] Route: `/reset-password/` — read `?uid=...&token=...` or hash.\n* [ ] `RestorePassword` component with fields: New password, Confirm password.\n* [ ] Client-side validation: required, match, min length, show strength hints.\n* [ ] POST to `/api/auth/password-reset/confirm/`; handle responses:\n\n  * success → show confirmation card (link to login + optional auto-redirect)\n  * invalid/expired token → show friendly error & CTA `Resend reset email`\n  * password complexity errors → show inline messages\n* [ ] Accessibility: labels, aria-live for server messages.\n* [ ] Unit tests (RTL): token parsing, validation, server error rendering.\n\n**Acceptance criteria**\n\n* Token pre-filled when present and input focused.\n* Strong password + valid token → success message & link to login.\n* Invalid/expired token shows CTA for resend.\n* Tests passing.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Token storage / single-use & lifecycle management",
    "body": "**Title:** backend: implement token lifecycle (stateful tokens or issuance tracking) and admin tools\n\n**Description (short):**\nEnsure tokens can be invalidated / marked as used. Provide ability for admin to inspect/revoke tokens and metrics for monitoring suspicious activity.\n\n**Checklist**\n\n* [ ] If using stateful tokens: create `PasswordResetToken` model (`user`, `token_hash`, `created_at`, `expires_at`, `used_at`, `revoked`).\n* [ ] If using stateless tokens: record issuance events with unique nonce and check nonce on confirm to prevent reuse.\n* [ ] Implement single-use enforcement (token cannot be reused).\n* [ ] Admin view or management command to list/revoke tokens.\n* [ ] Metrics: counters for `requests`, `confirms`, `expired_attempts`, `reused_attempts`.\n* [ ] Tests for lifecycle behavior.\n\n**Acceptance criteria**\n\n* Token confirmed once, subsequent attempts fail.\n* Admin can revoke outstanding tokens.\n* Tests cover revocation and reuse attempts.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: `POST /api/auth/password-reset/confirm/` (validate token + set password)",
    "body": "**Title:** backend: implement password reset confirm endpoint (token + uid + new password)\n\n**Description (short):**\nImplement endpoint that accepts token (and uid), validates it (single-use & expiry), enforces password complexity, updates user password, revokes refresh tokens/sessions, and logs the operation.\n\n**API spec**\n\n* **POST** `/api/auth/password-reset/confirm/`\n* **Request**\n\n```json\n{\n  \"uid\": \"encoded_user_id\",\n  \"token\": \"token_string\",\n  \"password\": \"NewP@ssw0rd!\"\n}\n```\n\n* **Responses**\n\n  * `200 OK` `{ \"detail\": \"Password changed successfully.\" }`\n  * `400 Bad Request` `{ \"detail\": \"Invalid or expired token.\" }`\n  * `422 Unprocessable Entity` `{ \"password\": [\"Password too weak.\"] }`\n\n**Checklist**\n\n* [ ] Verify token & uid using same scheme as request endpoint.\n* [ ] Enforce server-side password complexity (match registration rules).\n* [ ] `set_password()` and save; revoke refresh tokens (blacklist) and optionally expire active sessions.\n* [ ] Mark token as used (single-use).\n* [ ] Unit tests: success, invalid token, expired token, weak password.\n* [ ] Add audit log entry for resets (user_id, ip, user_agent, timestamp).\n\n**Acceptance criteria**\n\n* Valid token + strong password → 200 and user can login with new password.\n* Invalid/expired/used token → 400 and no password change.\n* Refresh tokens invalidated after password change.\n* Tests passing.",
    "labels": [
      "task",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Restore Password (Set New Password) desktop",
    "body": "**Short description:**\nImplement the “Restore password / Set new password” desktop flow from the mockups: token parsing from link, secure token verification, new password form with client & server validation, single-use tokens, friendly UX (success, invalid/expired token, resend), logging and tests. ",
    "labels": [
      "epic",
      "restore password"
    ],
    "state": "open"
  },
  {
    "title": "Security review & docs: anti-enumeration, CSRF, CORS, token storage guidance",
    "body": "**Title:** security: perform security checklist and document secure reset flow decisions\n\n**Description:**\nPerform a short security review for the reset flow (anti-enumeration, CSRF when cookies used, CORS allowed origins, XSS in emails), and document recommended token storage/usage patterns for frontend/back. Add guidance for production email provider secrets and monitoring.\n\n**Checklist**\n\n* [ ] Verify `POST /api/auth/password-reset/` returns 200 for existing/non-existing email.\n* [ ] Ensure password-reset confirm endpoint enforces token expiry and single-use.\n* [ ] Document CORS allowed origins and ensure only frontend origin allowed for auth endpoints.\n* [ ] If you plan to store tokens in cookies, document CSRF protections (SameSite, HttpOnly).\n* [ ] Ensure reset token link does not expose sensitive data in logs (redact).\n* [ ] Provide a short “How to configure secure email provider” guide and required secrets.\n* [ ] Add monitoring list: elevated reset requests (alert), failed confirm attempts, token reuse attempts.\n\n**Acceptance criteria**\n\n* Security checklist completed and committed to `docs/security/forgot-password.md`.\n* Clear instructions provided for production deployment (CORS, email provider secrets, token TTL).\n* Monitoring & alerting recommendations present.\n",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "QA & E2E: End-to-end tests and accessibility for forgot/reset flows",
    "body": "**Title:** qa: E2E and accessibility tests for Forgot Password flow\n\n**Description:**\nCreate automated end-to-end tests that simulate the full flow (request -> email capture -> follow link -> set password) plus tests for failure cases (expired token, invalid token). Run Lighthouse or axe checks for accessibility on both pages.\n\n**Checklist**\n\n* [ ] Implement E2E tests (Cypress/Playwright) that cover:\n\n  * Requesting reset (assert success message)\n  * Following tokenized link (using token from DB or fixture) → setting new password → login with new password\n  * Invalid token flow (assert error message)\n  * Throttle scenario (simulate 429 from backend)\n* [ ] Accessibility checks with `axe` or Lighthouse; fix critical issues.\n* [ ] Add E2E to CI and run on PRs affecting auth.\n* [ ] Generate QA doc with test steps and screenshots.\n\n**Acceptance criteria**\n\n* E2E tests run in CI and pass.\n* Accessibility scan reports no critical failures (WCAG A/A+ recommended).\n* QA doc stored in `docs/qa/forgot-password.md`.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: UX details — deep link behavior, auto-redirects & friendly copy",
    "body": "**Title:** frontend: implement deep-link handling and UX polish for reset flow\n\n**Description:**\nImplement behavior around clicking the email link: parse token from URL, focus appropriate input, handle expired token gracefully, and provide friendly copy. Optionally offer auto-login after reset (only if backend supports secure token exchange).\n\n**Checklist**\n\n* [ ] When token present, open Reset Confirm page with token prefilled (hidden field).\n* [ ] Autofocus new password input and show password rules block.\n* [ ] On success, display a small confirmation modal/notice and auto-redirect to `/login` after N seconds (configurable) or provide “Go to login” button.\n* [ ] If token invalid/expired, show CTA “Resend reset email” that pre-fills their email in request form.\n* [ ] Accessibility: ensure aria-live announcements for errors and success messages.\n* [ ] Unit tests for deep-link parsing & redirect behavior.\n\n**Acceptance criteria**\n\n* Clicking email link lands user on reset form with token parsed and input focused.\n* Token errors show clear call to action to request a new reset.\n* Auto-redirect works and is cancellable by user if they want to copy something.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Reset Confirm Page (desktop) — set new password UI & token handling",
    "body": "**Title:** frontend: implement Password Reset Confirm page (token + new password)\n\n**Description:**\nCreate page that reads token (and uid) from URL or allows paste, validates password strength & confirm password, posts to confirm endpoint, and shows success/error states per mockup (invalid/expired token, weak password, success redirect to login).\n\n**Checklist**\n\n* [ ] Create `PasswordResetConfirm` component at `/reset-password/?token=xxx&uid=...` (support hash or query).\n* [ ] Parse token & uid from URL; allow manual paste fallback.\n* [ ] Client-side password rules: min length, mixed chars; show strength hints, confirm password field.\n* [ ] POST to `/api/auth/password-reset/confirm/` (or mock); handle responses:\n\n  * success -> show confirmation and link to login\n  * invalid/expired token -> show friendly error and \"Resend reset email\" action\n  * password complexity -> show inline errors\n* [ ] Unit tests for flow and error states.\n\n**Acceptance criteria**\n\n* Valid token + strong password → success page and redirect link to login works.\n* Invalid/expired token → user sees helpful message and CTA to request a new reset email.\n* Password complexity enforced client-side and server-side errors are shown correctly.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Password Reset Request Page (desktop) — UI + client validation",
    "body": "**Title:** frontend: implement Password Reset request page (desktop)\n\n**Description:**\nBuild the desktop page per mockup where user enters their email and clicks submit. Implement client-side validation and show required UI states: idle, invalid email highlight, success message (instructing to check inbox), rate-limit error display, and generic server error.\n\n**Checklist**\n\n* [ ] Create `PasswordResetRequest` component and route `/forgot-password`.\n* [ ] Client-side validation: required, correct email format.\n* [ ] On submit, POST to `/api/auth/password-reset/` (or mock) and show the success notice regardless of backend response.\n* [ ] Show inline field error (red) for invalid email and server inline messages if backend returns 429.\n* [ ] Add accessible markup (labels, aria-live for messages).\n* [ ] Unit tests (React Testing Library): validation, success message rendering.\n\n**Acceptance criteria**\n\n* Invalid email shows inline validation and prevents API call.\n* Submitting valid email shows success message telling user to check inbox (even for unknown email).\n* If backend responds 429, show throttle message advising to retry later.\n* Component tests pass.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Email templates & delivery integration (dev & staging)",
    "body": "**Title:** backend: create email templates and integrate email delivery (console/dev + provider)\n\n**Description:**\nCreate HTML and plain-text templates for the reset email (desktop-friendly), configure email backend for dev (console) and staging (SMTP/SES), and allow testing via a staging setting.\n\n**Checklist**\n\n* [ ] Design responsive HTML email template for password reset with clear CTA and token link (desktop-first).\n* [ ] Provide plain-text fallback.\n* [ ] Implement template rendering with context: `user_name`, `reset_link`, `expiry_minutes`.\n* [ ] Add email settings: `EMAIL_BACKEND`, sender, reply-to, template variables.\n* [ ] Document how to test emails locally (console backend) and how to set provider credentials in env.\n* [ ] Unit/test: verify email body contains correct link and token.\n\n**Acceptance criteria**\n\n* Reset email is rendered in HTML & text and contains working link template.\n* Developers can test via console backend; staging can send via provider when configured.\n* Template content and branding match the mockup tone.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Token lifecycle, revocation and monitoring",
    "body": "**Title:** backend: implement password-reset token lifecycle & revocation support + audit logs\n\n**Description:**\nProvide token lifecycle management so tokens can be invalidated (e.g., if user requests multiple resets) and add audit entries for resets and revoke actions. Provide admin commands to inspect / revoke outstanding tokens.\n\n**Checklist**\n\n* [ ] If tokens are stateless (signed), implement short TTL and store token issuance events in DB to allow detection of multi-request patterns.\n* [ ] If tokens are stateful, store token rows (token, user_id, created_at, expires_at, used_flag) and use DB lookup in confirm endpoint.\n* [ ] Implement automatic marking token as `used` on successful reset.\n* [ ] Admin management CLI or admin view to list/revoke tokens.\n* [ ] Add monitoring metrics (counter for reset requests, confirms, expired tokens).\n* [ ] Unit tests for token marking, revoke, and used detection.\n\n**Acceptance criteria**\n\n* Token cannot be used more than once.\n* Revoked tokens fail confirm.\n* Admin can view outstanding tokens and revoke them.\n* Tests for lifecycle behavior exist.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Password reset confirm endpoint `POST /api/auth/password-reset/confirm/`",
    "body": "**Title:** backend: implement password reset confirm endpoint (token + new password)\n\n**Description:**\nImplement endpoint to accept token and new password, validate token, apply password complexity rules, update user password, revoke existing login sessions (optionally), and log the event.\n\n**API (spec):**\n\n* `POST /api/auth/password-reset/confirm/`\n* Request:\n\n```json\n{ \"token\": \"<token>\", \"uid\": \"<user-id-or-encoded>\", \"password\": \"NewP@ssw0rd\" }\n```\n\n* Response: `200 OK` `{ \"detail\": \"Password changed successfully.\" }` or `400` with error message.\n\n**Checklist**\n\n* [ ] Implement token verification and extract user (use same method as Issue 1).\n* [ ] Validate password complexity server-side (consistent with registration rules).\n* [ ] Save new password using `set_password()` and rotate/revoke refresh tokens (if used).\n* [ ] Log the reset event in audit logs.\n* [ ] Unit tests: valid token -> password changes, invalid/expired token -> 400, complexity fail -> 400.\n* [ ] Add rate limiting for confirm attempts (prevent token brute-force).\n\n**Acceptance criteria**\n\n* Valid token + password -> 200 and user can authenticate with new password.\n* Invalid/expired token -> 400 with non-sensitive error message.\n* Tests cover happy and error paths.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Password reset request endpoint `POST /api/auth/password-reset/`",
    "body": "**Title:** backend: implement password-reset request endpoint\n\n**Description:**\nImplement endpoint that accepts an email and *always* returns a success response while internally sending a time-limited reset token email when the account exists (no account enumeration). Use Django’s `PasswordResetTokenGenerator` or signed tokens. Add rate-limiting and logging.\n\n**API (spec):**\n\n* `POST /api/auth/password-reset/`\n* Request:\n\n```json\n{ \"email\": \"user@example.com\" }\n```\n\n* Response: `200 OK`\n\n  ```json\n  { \"detail\": \"If the email exists, you will receive reset instructions.\" }\n  ```\n\n**Checklist**\n\n* [ ] Implement serializer & view.\n* [ ] Generate secure, time-limited token (recommended: Django `PasswordResetTokenGenerator` or signed JWT with short TTL).\n* [ ] Store minimal metadata for token issuance (timestamp, user id) in audit table (for later monitoring and revocation).\n* [ ] Send reset email (use email service abstraction to support console/dev and SMTP/SES).\n* [ ] Add throttling (per IP, per email) — e.g., 5 requests per hour.\n* [ ] Unit tests: request returns 200 for known/unknown email; token created for known user; throttle triggers properly.\n* [ ] Document email payload and link template in README/OpenAPI.\n\n**Acceptance criteria**\n\n* Endpoint always returns 200 and message as above.\n* Known user triggers an email with a valid link containing token that expires (e.g., 1 hour).\n* Rate limiting prevents abuse and unit tests cover logic.",
    "labels": [
      "task",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "Forgot Password / Reset Flow (Desktop)",
    "body": "**Description — short:**\nImplement full desktop \"Forgot password\" experience from mockups: request reset, email with token/link, set new password, resend and error states. Backend must provide secure token handling, rate-limiting, logging and email templates. Frontend must implement the desktop pages, client-side validation, and graceful UX for all edge states. \n\n**Epic-level acceptance criteria**\n\n* Desktop \"Forgot password\" request page and “Set new password” page exist and match mockup states (idle, validation error, server error, success).\n* Backend endpoints exist, documented, and covered by unit tests (request, confirm, token handling, resend).\n* Rate limiting and anti-enumeration protections are in place.\n* E2E tests for the main happy path + at least 2 error scenarios run in CI.\n* Email templates exist and are testable (console + staging email provider).",
    "labels": [
      "epic",
      "forgot password"
    ],
    "state": "open"
  },
  {
    "title": "QA / Security / E2E: Visual QA, accessibility and End-to-End tests for Login flows",
    "body": "**Title:** qa: E2E + accessibility + security checklist for login flow (Cypress/Playwright + Lighthouse)\n\n**Description:**\nCreate automation and QA artifacts: E2E tests that exercise login, password reset, email verification flows; run Lighthouse/A11y checks; perform basic security review (CSRF, token storage, XSS, CORS).\n\n**Checklist**\n\n* [ ] Create E2E tests:\n\n  * Login success & redirect.\n  * Login failure (401 shows error).\n  * Password reset request and confirm flow (simulate token).\n  * Email verification confirm flow.\n* [ ] Run Lighthouse accessibility for desktop and record report.\n* [ ] Verify keyboard accessibility (tab order, focus outlines) for login form.\n* [ ] Security checklist:\n\n  * Verify auth endpoints require HTTPS in docs.\n  * Verify sensitive tokens are not logged in client console.\n  * Verify CORS config allows only frontend origin.\n  * Verify CSRF protections if cookies used.\n* [ ] Add E2E to CI (runs on PR) and create a `docs/qa/login-flow.md` with results.\n\n**Acceptance criteria**\n\n* E2E tests run in CI and pass (or documented mock usage accepted).\n* Accessibility report shows no critical failures; show fixes for major issues.\n* Security checklist items documented and flagged if action required.\n* QA docs committed.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Email verification UI & resend (post-registration)",
    "body": "**Title:** frontend: implement Email verification notice page and resend flow\n\n**Description:**\nAfter registration, show a verification page instructing the user to check email, allow resend, and a manual verify input (token paste) for desktop.\n\n**Checklist**\n\n* [ ] Implement `VerifyEmailNotice` component shown after register.\n* [ ] Implement `ResendVerification` button that calls `/api/auth/resend-verification/` (show throttle feedback).\n* [ ] Implement `VerifyEmailConfirm` component to POST token to `/api/auth/verify-email/`.\n* [ ] Unit tests for UI states and resend success/limit messages.\n\n**Acceptance criteria**\n\n* Resend button triggers API and shows success/toast; if rate-limited show message.\n* Verify token properly calls API and updates UI to \"verified\" and redirects to login.\n* Tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Password reset UI (request + new password)",
    "body": "**Title:** frontend: implement Password Reset flow (desktop UI for request & confirm)\n\n**Description:**\nImplement two screens: request reset (enter email) and set new password (token in URL). Provide UX messages per mockup and validation for new password.\n\n**Checklist**\n\n* [ ] `PasswordResetRequest` component: form to POST `/api/auth/password-reset/` (mockable).\n* [ ] `PasswordResetConfirm` component: route `/reset-password/?token=...`, form to POST `/api/auth/password-reset/confirm/`.\n* [ ] Validate password strength & confirm password field.\n* [ ] Show success page instructing user to check email (always show success after POST).\n* [ ] Tests for flows and validation.\n\n**Acceptance criteria**\n\n* Request endpoint always returns success UI (no account enumeration).\n* Confirm with valid token triggers success UI and allows login.\n* Invalid/expired token shows helpful error and link to resend.\n* Tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Auth Context & Secure Token Storage; Protected routes",
    "body": "**Title:** frontend: implement Auth context, token handling, and ProtectedRoute wrapper\n\n**Description:**\nImplement central auth state: store tokens securely, refresh logic, automatic logout on expiry, and ProtectedRoute component to guard pages.\n\n**Notes / Options:**\n\n* Preferred: store refresh token in HttpOnly cookie (backend-set) and access token in memory. If backend gives tokens in JSON, then for this sprint use secure in-memory storage + refresh flow and document security tradeoffs.\n* At minimum implement token refresh before expiry.\n\n**Checklist**\n\n* [ ] Create `AuthProvider` with login, logout, and refresh methods.\n* [ ] Implement token storage strategy (in-memory + localStorage for “remember” option OR HttpOnly cookie integration if backend supports it). Document chosen approach.\n* [ ] Implement `ProtectedRoute` that redirects to `/login` if not authenticated.\n* [ ] Auto-refresh access token using `refresh` endpoint before expiry.\n* [ ] Unit tests for AuthProvider logic (mock timers to test refresh).\n\n**Acceptance criteria**\n\n* After successful login, app navigates to `/` (or intended redirect).\n* ProtectedRoute blocks access to protected page when not authenticated.\n* Token refresh works (mock call) and auto-logout occurs on invalid refresh.\n* Tests for core logic pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Login Page (desktop) — UI, validations & error states",
    "body": "**Title:** frontend: implement Login page component with client-side validation & UI states\n\n**Description:**\nCreate the desktop Login page per mockup with both field-level validation and server error handling. Provide states: idle, loading, validation errors, credential error, locked/too many attempts message.\n\n**Checklist**\n\n* [ ] Build `LoginPage` React component with input fields: Email, Password, Role dropdown? (use mock if role in form).\n* [ ] Client-side validation: required, email format, password min length.\n* [ ] Show field-level errors (red) exactly like mockup; success border on valid (green) when appropriate.\n* [ ] Implement \"Remember me\" checkbox and \"Forgot password\" link.\n* [ ] Integrate with `POST /api/auth/login/` (use API if backend ready or mock responses).\n* [ ] Show loading spinner on submit; show toast/inline error on 401 or 429.\n* [ ] Unit tests (Jest/Testing Library) for validation and state transitions.\n\n**Acceptance criteria**\n\n* Submitting invalid form shows client-side validation errors and prevents request.\n* Backend 401 shows credential error message from mockup.\n* Backend 429 / locked shows lock message.\n* Clicking \"Forgot password\" navigates to reset page.\n* Component tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Email verification & resend verification endpoint",
    "body": "**Title:** backend: implement email verification after registration + resend endpoint\n\n**Description:**\nSupport verifying newly registered users via email token and implement `/api/auth/resend-verification/`. Email must be configurable and tokens time-limited.\n\n**API spec**\n\n* `POST /api/auth/verify-email/` `{ \"token\": \"<token>\" }` → 200 (activates account).\n* `POST /api/auth/resend-verification/` `{ \"email\": \"user@mail\" }` → 200 (silent success).\n\n**Checklist**\n\n* [ ] Implement verification token generator and verification view.\n* [ ] Send email on registration with verification link.\n* [ ] Implement resend endpoint with rate limit.\n* [ ] Tests for verification success, resend rate-limiting, expired tokens.\n\n**Acceptance criteria**\n\n* Users receive verification email on register (or dev uses console backend).\n* `verify-email` activates account and returns 200; invalid token returns 400.\n* Resend endpoint returns 200 and throttle prevents abuse.\n* Tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Password reset flow (request + confirm)",
    "body": "**Title:** backend: implement password reset endpoints & email templates\n\n**Description:**\nImplement endpoints to request password reset (send email w/ token link) and to set a new password (confirm endpoint). Include safe tokens and expiry.\n\n**API spec**\n\n* `POST /api/auth/password-reset/` `{ \"email\": \"user@x.com\" }` → 200 (always return success to avoid account enumeration).\n* `POST /api/auth/password-reset/confirm/` `{ \"token\": \"<token>\", \"password\": \"NewP@ss1\" }` → 200.\n\n**Checklist**\n\n* [ ] Implement generating time-limited reset tokens (signed tokens or Django default `PasswordResetTokenGenerator`).\n* [ ] Implement email template for reset (HTML + plain) and a configurable sender.\n* [ ] Endpoint to accept token + new password and update password (validate complexity).\n* [ ] Unit tests for token generation, expiry, and reset success/failure.\n* [ ] Add audit/log entry for password resets.\n\n**Acceptance criteria**\n\n* Request endpoint always returns 200 (no account enumeration).\n* Valid token + password updates password and invalid/expired token returns 400.\n* Email contains link with token (link template documented).\n* Tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Token refresh & logout (`/api/auth/refresh/`, `/api/auth/logout/`)",
    "body": "**Title:** backend: implement refresh and logout endpoints (refresh, revoke)\n\n**Description:**\nProvide endpoint to refresh access token using refresh token and endpoint to logout/revoke refresh tokens (so users can be logged out remotely).\n\n**API spec**\n\n* `POST /api/auth/refresh/` request `{ \"refresh\": \"<jwt-refresh>\" }` response `{ \"access\": \"<jwt-access>\" }`.\n* `POST /api/auth/logout/` request `{ \"refresh\": \"<jwt-refresh>\" }` -> revokes the refresh token and returns 204.\n\n**Checklist**\n\n* [ ] Implement refresh using chosen JWT library (SimpleJWT recommended).\n* [ ] Implement refresh-token revocation (DB blacklist or token storage) for logout.\n* [ ] Add unit tests covering refresh success/failure and logout revocation.\n* [ ] Document token expiry policy (access TTL, refresh TTL).\n\n**Acceptance criteria**\n\n* `POST /api/auth/refresh/` issues new access when refresh valid.\n* `POST /api/auth/logout/` revokes refresh and subsequent refresh attempts fail.\n* Tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Implement `POST /api/auth/login/` (credentials → JWT)",
    "body": "**Title:** backend: implement login endpoint `POST /api/auth/login/` (returns access + refresh tokens)\n\n**Description:**\nImplement secure login endpoint to accept email + password and return JWT access & refresh tokens (or session cookie, see notes). Must include rate-limiting and simple brute-force protection.\n\n**API spec**\n\n* **Request:** `POST /api/auth/login/`\n\n```json\n{ \"email\": \"user@example.com\", \"password\": \"P@ssw0rd\", \"remember\": true }\n```\n\n* **Response 200:**\n\n```json\n{ \"access\": \"<jwt-access>\", \"refresh\": \"<jwt-refresh>\", \"user\": { \"id\": 1, \"email\":\"user@example.com\", \"role\":\"startup\" } }\n```\n\n* **Errors:** 400 for validation, 401 for bad credentials.\n\n**Checklist**\n\n* [ ] Implement serializer & viewset using DRF.\n* [ ] Use secure password check (Django `authenticate`).\n* [ ] Issue short-lived access token and longer refresh token (JWT or DRF SimpleJWT).\n* [ ] Add rate-limiting (DRF throttle or custom) + account lockout after N failed attempts.\n* [ ] Add unit tests for success, invalid credentials, locking behavior.\n* [ ] Document response shape in README or OpenAPI.\n\n**Acceptance criteria**\n\n* Valid credentials → 200 with tokens and user payload.\n* Wrong password → 401 and does not reveal which field failed.\n* Excessive failed attempts → 429 or locked response per policy.\n* Unit tests pass.",
    "labels": [
      "task",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Desktop Login Flow (Authentication — Login, Password Reset, Email Verification)",
    "body": "**Description:**\nImplement desktop-only authentication flows per mockups: login page, client-side validation/error states, token/session handling, password reset (request + set), email verification and resend, plus backend endpoints, tests and QA. All features must support secure token handling and graceful UI for all validation/edge cases.\n\n**Acceptance criteria (epic-level):**\n\n* Desktop login page renders and functions per mockup (idle, validation errors, success).\n* Backend endpoints exist for login, refresh, logout, password reset, and email verification (documented).\n* Frontend stores/authenticates tokens securely (see ticket) and Protected routes work.\n* E2E smoke-tests for core flows exist and pass in CI.\n\nMockup https://github.com/Project-Stage-Academy/Forum-PrSt-FullStack/blob/main/mockups/Log%20In%20Flow.png\n\n",
    "labels": [
      "epic",
      "login flow"
    ],
    "state": "open"
  },
  {
    "title": "Integration & QA: Visual review, accessibility checks, cross-browser testing (desktop)",
    "body": "**Title:** qa: integration, visual QA and accessibility for desktop landing page\n\n**Description:**\nIntegrate the implemented components on the main route `/`, run visual review vs mockup, fix high-priority accessibility issues (contrast, alt text, aria roles) and prepare a QA checklist.\n\n**Checklist**\n\n* [ ] Merge frontend components into route `/`.\n* [ ] Create a visual QA checklist (screenshots, differences).\n* [ ] Run Lighthouse accessibility and performance checks (desktop).\n* [ ] Fix critical accessibility items (missing alt, low contrast, missing landmarks).\n* [ ] Cross-browser check: Chrome & Firefox latest (desktop).\n* [ ] Add end-to-end smoke test (Cypress or Playwright) that opens `/`, asserts hero text, and presence of 8 startup cards.\n\n**Acceptance criteria**\n\n* `/` renders with all components and no blocking accessibility issues (WCAG AA recommended).\n* E2E smoke test passes in CI on the deployed review app (or CI container).\n* Visual checklist document added to repo `docs/visual-qa/`.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Footer component with links and subscribe box",
    "body": "**Title:** frontend: implement Footer (desktop) — links, contacts, copyright\n\n**Description:**\nCreate the footer as in mockup: dark background, site links, contact info, social links, and an email subscribe input.\n\n**Checklist**\n\n* [ ] Create `Footer` component and styles.\n* [ ] Load link lists from `/api/content/landing/` (or use local mock).\n* [ ] Implement subscribe form that POSTs to `/api/subscribe/` (endpoint can be mocked for now).\n* [ ] Ensure footer is keyboard accessible and responsive for desktop.\n\n**Acceptance criteria**\n\n* Footer renders with link groups and contact info.\n* Subscribe attempts to POST (mock) and shows success/error toast.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: \"Why worth\" information grid",
    "body": "**Title:** frontend: implement \"Why worth\" info blocks grid (desktop)\n\n**Description:**\nImplement the information blocks section (\"Чому варто\" / Why worth) that shows several rectangular blocks with heading and short copy.\n\n**Checklist**\n\n* [ ] Create `WhyWorthGrid` component and styles.\n* [ ] Load content from `/api/content/landing/` or fallback to mock.\n* [ ] Make each block accessible and keyboard-focusable.\n* [ ] Add basic unit tests.\n\n**Acceptance criteria**\n\n* Grid displays 4–6 info blocks matching mockup copy & layout.\n* Blocks load from API when available and show placeholder UI during loading.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: CTA strip & “For whom” cards row",
    "body": "**Title:** frontend: implement CTA strip (green) and \"For whom\" cards per mockup\n\n**Description:**\nAdd the green call-to-action strip and the set of cards showing user segments (icons + labels). Desktop-only layout with grid of cards.\n\n**Checklist**\n\n* [ ] Create `CTASection` component (green band, center CTA button).\n* [ ] Create `ForWhomGrid` component that loads card list from `/api/content/landing/` or local mock.\n* [ ] Implement icons (SVG sprite or inline) and hover effects.\n\n**Acceptance criteria**\n\n* CTA strip appears between hero and cards and CTA triggers a route (`/register`).\n* ForWhomCards render 6–8 cards with icon and title per mockup.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: \"New participants\" grid and card component",
    "body": "**Title:** frontend: implement Startups grid + StartupCard component (desktop)\n\n**Description:**\nCreate the startups grid (cards with image, title, short desc, tags) that displays results from `GET /api/startups/`. Desktop-only grid with 4 per row (or per mockup).\n\n**Checklist**\n\n* [ ] Create `StartupCard` component (image, name, short_description, tags, CTA link).\n* [ ] Create `StartupsGrid` container component that fetches `/api/startups/?page=1&page_size=8`.\n* [ ] Implement pagination UI or \"View more\" button to fetch next page.\n* [ ] Add loading and empty states.\n* [ ] Add tests for component rendering and API integration (mock fetch).\n\n**Acceptance criteria**\n\n* Startups grid displays up to 8 cards on initial load.\n* Clicking a card navigates to `/startups/:id` (route may be placeholder).\n* Grid fetches data from backend if available; otherwise uses fixture/mock.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Hero section with image collage & CTA",
    "body": "**Title:** frontend: implement Hero section — title, subtitle, image collage, CTA button\n\n**Description:**\nBuild the hero section as per mockup: large title, CTA button, and a collage/stack of images on the right. Desktop-only, responsive behaviour not required now.\n\n**Checklist**\n\n* [ ] Create `Hero` React component and styles.\n* [ ] Load hero content from `/api/content/landing/` (fallback to local mock JSON if endpoint not available).\n* [ ] Implement CTA button that navigates to `/register`.\n* [ ] Ensure images have `alt` attributes and lazy-load.\n\n**Acceptance criteria**\n\n* Hero displays title, subtitle, CTA and collage on desktop.\n* CTA navigates to `/register`.\n* Hero uses content from API when available; otherwise mock JSON.",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: Header & navigation (desktop)",
    "body": "**Title:** frontend: implement Header & navigation (desktop) — logo, search, auth links\n\n**Description:**\nCreate React header component matching mockup (logo left, search box, top navigation links, \"Login/Register\" actions). Desktop-only behaviour: full nav visible, no mobile collapse.\n\n**Checklist**\n\n* [ ] Create `Header` component and `Header.scss` (or styled system).\n* [ ] Implement search input (UI only) and placeholder `onSearch` that navigates to `/search?q=...`.\n* [ ] Add auth buttons: `Login` and `Register` (links route to `/login` `/register`).\n* [ ] Integrate with global Auth context (if not available, use placeholder that shows logged-out state).\n* [ ] Add unit/storybook story (optional) or snapshot test.\n\n**Acceptance criteria**\n\n* Header renders on `/` and matches layout in mockup (desktop).\n* Clicking logo navigates to `/`.\n* Search input triggers navigation to `/search?q=...` (router push).",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Content API for static sections",
    "body": "**Title:** backend: implement `GET /api/content/landing/` — hero text, CTA, \"for whom\" cards, and \"why worth\" blocks\n\n**Description:**\nProvide a simple JSON endpoint with the text, cards and links used in the static parts of the page so frontend can render content without hardcoding.\n\n**API spec**\n\n* `GET /api/content/landing/` response example:\n\n```json\n{\n  \"hero\": { \"title\": \"...\", \"subtitle\": \"...\", \"cta_text\":\"Join\", \"hero_images\": [...] },\n  \"for_whom\": [ { \"icon\": \"designer\", \"title\": \"...\", \"desc\":\"...\" }, ... ],\n  \"why_worth\": [ { \"title\": \"...\", \"desc\": \"...\" }, ... ],\n  \"footer_links\": { \"left\":[...], \"right\":[...] }\n}\n```\n\n**Checklist**\n\n* [ ] Create `LandingContent` model or simple config-backed view that returns JSON.\n* [ ] Populate with initial data (admin-editable optional).\n* [ ] Add unit tests verifying JSON structure.\n\n**Acceptance criteria**\n\n* `GET /api/content/landing/` returns the JSON shape above.\n* Frontend can render page using this endpoint (or fallback).",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Backend: Startups list API (public)",
    "body": "**Title:** backend: implement `GET /api/startups/` (list) with pagination, tags, and sample fixture\n\n**Description:**\nCreate the API returning “new participants” (startups) list to power the grid on the landing page.\n\n**API spec**\n\n* `GET /api/startups/?page=1&page_size=8&tag=craft`\n* Response:\n\n```json\n{\n  \"count\": 123,\n  \"next\": \"url\",\n  \"previous\": null,\n  \"results\": [\n    {\n      \"id\": 1,\n      \"company_name\": \"Handmade Co\",\n      \"short_description\": \"Woodwork and ceramics\",\n      \"thumbnail_url\": \"/media/thumbs/1.jpg\",\n      \"location\": \"Chernivtsi\",\n      \"tags\": [\"craft\", \"pottery\"]\n    },\n    ...\n  ]\n}\n```\n\n**Checklist**\n\n* [ ] Create serializer and viewset (DRF `ListAPIView` or `ModelViewSet` read-only).\n* [ ] Add simple filtering by `tag` and search by `company_name`.\n* [ ] Paginate (default page_size=8).\n* [ ] Add fixture `fixtures/startups.json` with 8–12 sample startup records (images may be placeholders).\n* [ ] Add unit tests for endpoint (list, pagination, filter).\n\n**Acceptance criteria**\n\n* `GET /api/startups/` returns 200 with `results` length ≤ `page_size`.\n* Filtering by `tag` returns only matching startups.\n* Sample fixture loads successfully (`loaddata`).",
    "labels": [
      "task",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Implement Desktop Landing Page (Frontend + Backend)",
    "body": "**Description:**\nImplement full desktop-only landing page per mockup (header, hero, “New participants” grid, CTA strip, “For whom” cards, “Why worth” grid, footer). Backend: provide APIs and sample data. Frontend: React desktop components, styling, and integration with APIs or mocks.\n\n**Acceptance criteria**\n\n* All below issues are completed and PRs merged.\n* Desktop page in `frontend/` at route `/` matches mockup structure (visual parity acceptable; pixel-perfect not required).\n* Basic tests + CI green for affected modules.\n\nMockup https://github.com/Project-Stage-Academy/Forum-PrSt-FullStack/blob/main/mockups/before%20log%20in.png",
    "labels": [
      "epic",
      "landing page"
    ],
    "state": "open"
  },
  {
    "title": "Initial Project Setup",
    "body": "* **Epic:** `Initial Project Setup`\n",
    "labels": [
      "epic"
    ],
    "state": "open"
  },
  {
    "title": "Codecov, badges & dependency automation",
    "body": "**Title:** quality: integrate Codecov, add repo badges and enable Dependabot\n\n**Description:**\nSet up code coverage reporting and basic automation tools to keep repository healthy.\n\n**Checklist**\n\n* [ ] Add `codecov` upload step to CI workflow (use placeholder token or document how to add token).\n* [ ] Add badges to `README.md`: build status, coverage (Codecov), code quality (optional).\n* [ ] Create `.github/dependabot.yml` to enable Dependabot for GitHub Actions, npm, and pip.\n* [ ] Add basic `security` and `dependabot` documentation in README.\n\n**Acceptance criteria**\n\n* README shows CI badge and Codecov badge placeholders.\n* Dependabot config is present and valid.\n* CI run includes Codecov step (may fail until token provided) — documented.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "Tests & coverage baseline",
    "body": "\n**Title:** tests: add pytest setup and baseline unit tests + coverage config\n\n**Description:**\nAdd testing framework, sample tests for core models/endpoints, and configure coverage reporting.\n\n**Checklist**\n\n* [ ] Add `pytest` and `pytest-django` to `requirements-dev.txt`.\n* [ ] Create `pytest.ini` with Django settings and markers.\n* [ ] Add sample tests:\n\n  * `tests/test_models.py` — User and StartupProfile basic tests.\n  * `tests/test_health.py` — `GET /api/health/`.\n* [ ] Add coverage config and `run_coverage` script.\n* [ ] Add `codecov` upload step to CI (token can be set as secret later).\n* [ ] Ensure at least one test passes in CI.\n\n**Acceptance criteria**\n\n* Running `pytest` locally runs tests and returns success.\n* CI runs pytest and outputs coverage artifact.\n* Sample tests cover at least basic models and health endpoint.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "CI pipeline: GitHub Actions for tests, lint, and build",
    "body": "**Title:** ci: add GitHub Actions workflow (lint, tests, frontend build)\n\n**Description:**\nCreate CI pipeline that runs linters, Django tests, and frontend build for every PR and pushes results.\n\n**Checklist**\n\n* [ ] Add `.github/workflows/ci.yml` that runs:\n\n  * Python environment: install requirements, run linters, run `pytest`.\n  * Node environment: install deps, run `npm run build` (or `yarn build`).\n* [ ] Configure matrix for Python versions if needed (optional).\n* [ ] Add a status check that blocks merging if CI fails.\n* [ ] Add documentation how to read CI logs.\n\n**Acceptance criteria**\n\n* A PR triggers the workflow and status is displayed in GitHub checks.\n* When tests fail, the workflow shows failing step.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "Linting, formatting and pre-commit",
    "body": "**Title:** ci: add linting & formatting: pylint/flake8, black, pre-commit hooks\n\n**Description:**\nSet up code style & automated local checks so PRs follow consistent quality rules.\n\n**Checklist**\n\n* [ ] Add `pyproject.toml` / config for `black` and `isort` (or `setup.cfg`).\n* [ ] Add `pylint` or `flake8` config and baseline run.\n* [ ] Add `eslint` config for frontend skeleton.\n* [ ] Install and configure `pre-commit` with hooks: `black`, `isort`, `flake8`/`pylint`, commit-msg (optional).\n* [ ] Document how to run linters locally.\n* [ ] Commit configuration and demonstrate a sample lint pass/fix.\n\n**Acceptance criteria**\n\n* `pre-commit install` succeeds.\n* Staging and committing a Python file triggers formatting and lint checks locally.\n* Lint config is documented in repo README.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "DB schema & Django models (initial)",
    "body": "**Title:** backend: implement initial DB schema and Django models + migrations\n\n**Description:**\nImplement the initial data model for the project (users, startup profile, investor profile, project, message, saved startup).\n\n**Checklist**\n\n* [ ] Define models: `User` (or extend `AbstractUser`), `StartupProfile`, `InvestorProfile`, `Project`, `Message`, `SavedStartup`, `Notification`.\n* [ ] Add basic fields as agreed in BA mapping (email, role, company_name, description, project fields, etc.).\n* [ ] Add model constraints and indexes where appropriate.\n* [ ] Create and run initial migrations.\n* [ ] Add a small fixture or `loaddata` JSON with 2 example users (1 startup, 1 investor) and 1 project.\n* [ ] Add model unit tests verifying field presence and simple relations.\n\n\n\n**Acceptance criteria**\n\n* `python manage.py migrate` completes and creates tables.\n* Example fixture loads without errors and GET endpoints show created records (via admin or simple view).\n* Unit tests for models pass locally.\n",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "Docker & docker-compose for local dev",
    "body": "**Title:** infra: add Dockerfiles and docker-compose for backend, frontend, Postgres, Redis\n\n**Description:**\nProvide containerized local environment for consistent development across machines.\n\n**Checklist**\n\n* [ ] Create `Dockerfile.backend` for Django Python app.\n* [ ] Create `Dockerfile.frontend` for React app (dev mode recommended).\n* [ ] Add `docker-compose.yml` with services: `backend`, `frontend`, `db` (Postgres), `redis` (optional for channels/notifications).\n* [ ] Add environment variable examples `env.example` for backend (DB_HOST, DB_USER...).\n* [ ] Document `docker-compose up --build` steps in README.\n* [ ] Ensure migrations run in compose (`command` or docs).\n\n**Acceptance criteria**\n\n* `docker-compose up --build` starts containers (backend may print migration errors but should start).\n* Postgres container is accessible from backend container with provided env vars.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "Frontend: React (Vite) scaffold & routing",
    "body": "**Title:** frontend: create React (Vite) app with basic pages and routing\n\n**Description:**\nInitialize the React frontend app and basic route structure so frontend devs can wire UI to APIs.\n\n**Checklist**\n\n* [ ] Create React app using Vite (`frontend/`) with TypeScript (or JS per team decision).\n* [ ] Install React Router and set up routes: `/`, `/login`, `/register`, `/startups/:id`, `/dashboard`, `/messages`.\n* [ ] Create skeleton pages/components: `Home`, `Login`, `Register`, `StartupView`, `InvestorDashboard`, `Inbox`.\n* [ ] Add `README` in `frontend/` with start/build commands.\n* [ ] Add `.env.example` with `VITE_API_URL` placeholder.\n* [ ] Commit branch `frontend/scaffold`.\n\n**Acceptance criteria**\n\n* `npm run dev` (or `yarn`) starts Vite and the home page loads on `http://localhost:PORT`.\n* Router navigates between skeleton pages without errors.",
    "labels": [
      "task"
    ],
    "state": "open"
  },
  {
    "title": "Django + DRF project scaffold",
    "body": "**Title:** backend: scaffold Django project with DRF and core apps\n\n**Description:**\nCreate the backend scaffold: Django project + apps and baseline settings for development.\n\n**Checklist**\n\n* [ ] Create Django project (e.g. `project/`) and app packages: `users`, `startups`, `investors`, `projects`, `messages`, `dashboard`.\n* [ ] Add `djangorestframework` to `requirements.txt`.\n* [ ] Create base settings for dev (split settings optional): dev database (Postgres placeholder env vars), installed apps, CORS, REST framework defaults.\n* [ ] Add `manage.py` tasks to runserver and migrations.\n* [ ] Add simple health-check endpoint `GET /api/health/` returning `{\"status\":\"ok\"}`.\n* [ ] Commit with descriptive message and push branch `backend/scaffold`.\n\n**Acceptance criteria**\n\n* `python manage.py runserver` runs without errors in a clean virtualenv (db connection can be placeholder).\n* `GET /api/health/` returns 200.\n* Apps are created and registered in settings.",
    "labels": [
      "task"
    ],
    "state": "open"
  }
]